diff --git a/entroly-core/src/context_receipts.rs b/entroly-core/src/context_receipts.rs
index 894f80c..0723759 100644
--- a/entroly-core/src/context_receipts.rs
+++ b/entroly-core/src/context_receipts.rs
@@ -113,10 +113,35 @@ pub struct OmittedContextItem {
 pub struct CompressionRatio {
     pub source_tokens: usize,
     pub selected_tokens: usize,
+    /// Physical source tokens not included in the selected context.
+    #[serde(default)]
+    pub tokens_withheld: usize,
+    /// Credited savings. Zero when retrieval found no evidence or selected nothing.
     pub tokens_saved: usize,
+    #[serde(default = "default_true")]
+    pub savings_eligible: bool,
+    #[serde(default = "default_legacy_savings_status")]
+    pub savings_status: String,
+    #[serde(default = "default_savings_baseline")]
+    pub savings_baseline: String,
     pub selected_to_source_ratio: f64,
     pub source_to_selected_ratio: f64,
+    /// Credited reduction percentage, not merely withheld percentage.
     pub reduction_pct: f64,
+    #[serde(default)]
+    pub withheld_pct: f64,
+}
+
+fn default_true() -> bool {
+    true
+}
+
+fn default_legacy_savings_status() -> String {
+    "legacy_mechanical_reduction".to_string()
+}
+
+fn default_savings_baseline() -> String {
+    "all_ingested_context".to_string()
 }
 
 #[derive(Clone, Debug, Serialize, Deserialize)]
@@ -831,7 +856,7 @@ fn select_context(
     let mut warnings = Vec::<String>::new();
 
     for rank in ranked {
-        if rank.final_score <= 0.0 && !selected_ids.is_empty() {
+        if rank.final_score <= 0.0 {
             continue;
         }
         let Some(chunk) = chunks.get(&rank.chunk_id) else {
@@ -970,7 +995,16 @@ fn select_context(
         }
     }
     if selected.is_empty() {
-        warnings.push("No chunks fit inside the token budget.".to_string());
+        if ranked.iter().any(|rank| rank.final_score > 0.0) {
+            warnings.push("No chunks fit inside the token budget.".to_string());
+            warnings.push(
+                "Relevant chunks were found, but none fit inside the token budget.".to_string(),
+            );
+        } else {
+            warnings.push(
+                "No relevant chunks matched the query; selection failed closed.".to_string(),
+            );
+        }
     }
     let unresolved = deps.iter().filter(|d| !d.resolved).count();
     if unresolved > 0 {
@@ -1007,25 +1041,50 @@ fn try_add(
     true
 }
 
-fn compression_ratio(source_tokens: usize, selected_tokens: usize) -> CompressionRatio {
+fn compression_ratio(
+    source_tokens: usize,
+    selected_tokens: usize,
+    savings_eligible: bool,
+    savings_status: &str,
+) -> CompressionRatio {
     if source_tokens == 0 {
         return CompressionRatio {
             source_tokens: 0,
             selected_tokens,
+            tokens_withheld: 0,
             tokens_saved: 0,
+            savings_eligible: false,
+            savings_status: "not_credited_no_source_context".to_string(),
+            savings_baseline: "all_ingested_context".to_string(),
             selected_to_source_ratio: 1.0,
             source_to_selected_ratio: 1.0,
             reduction_pct: 0.0,
+            withheld_pct: 0.0,
         };
     }
     let selected_ratio = selected_tokens as f64 / source_tokens as f64;
+    let tokens_withheld = source_tokens.saturating_sub(selected_tokens);
+    let withheld_pct = (1.0 - selected_ratio) * 100.0;
     CompressionRatio {
         source_tokens,
         selected_tokens,
-        tokens_saved: source_tokens.saturating_sub(selected_tokens),
+        tokens_withheld,
+        tokens_saved: if savings_eligible { tokens_withheld } else { 0 },
+        savings_eligible,
+        savings_status: savings_status.to_string(),
+        savings_baseline: "all_ingested_context".to_string(),
         selected_to_source_ratio: round6(selected_ratio),
-        source_to_selected_ratio: round6(source_tokens as f64 / selected_tokens.max(1) as f64),
-        reduction_pct: round3((1.0 - selected_ratio) * 100.0),
+        source_to_selected_ratio: if selected_tokens > 0 {
+            round6(source_tokens as f64 / selected_tokens as f64)
+        } else {
+            0.0
+        },
+        reduction_pct: if savings_eligible {
+            round3(withheld_pct)
+        } else {
+            0.0
+        },
+        withheld_pct: round3(withheld_pct),
     }
 }
 
@@ -1135,7 +1194,21 @@ pub fn build_receipt(
             (r.chunk_id.clone(), reasons)
         })
         .collect();
-    let ratio = compression_ratio(source_tokens, selected_tokens);
+    let relevance_evidence_found = ranked.iter().any(|rank| rank.final_score > 0.0);
+    let savings_eligible = relevance_evidence_found && !selected.is_empty();
+    let savings_status = if !relevance_evidence_found {
+        "not_credited_no_relevance_evidence"
+    } else if selected.is_empty() {
+        "not_credited_no_context_selected"
+    } else {
+        "mechanical_reduction_unverified"
+    };
+    let ratio = compression_ratio(
+        source_tokens,
+        selected_tokens,
+        savings_eligible,
+        savings_status,
+    );
     let risk_summary = risk_summary(
         index,
         selected.len(),
@@ -1189,7 +1262,11 @@ pub fn markdown_report(receipt: &ContextReceipt) -> String {
         format!("- Budget: {}", receipt.token_budget),
         format!("- Source tokens: {}", ratio.source_tokens),
         format!("- Selected tokens: {}", ratio.selected_tokens),
-        format!("- Reduction: {:.1}%", ratio.reduction_pct),
+        format!("- Tokens withheld: {}", ratio.tokens_withheld),
+        format!("- Credited token savings: {}", ratio.tokens_saved),
+        format!("- Savings status: {}", ratio.savings_status),
+        format!("- Credited reduction: {:.1}%", ratio.reduction_pct),
+        format!("- Physical withholding: {:.1}%", ratio.withheld_pct),
         format!(
             "- Source-to-selected ratio: {:.2}:1",
             ratio.source_to_selected_ratio
@@ -1441,6 +1518,67 @@ mod tests {
         assert_eq!(a.chunks[0].chunk_id, b.chunks[0].chunk_id);
     }
 
+
+    #[test]
+    fn legacy_compression_ratio_json_remains_deserializable() {
+        let legacy = serde_json::json!({
+            "source_tokens": 100,
+            "selected_tokens": 40,
+            "tokens_saved": 60,
+            "selected_to_source_ratio": 0.4,
+            "source_to_selected_ratio": 2.5,
+            "reduction_pct": 60.0
+        });
+        let ratio: CompressionRatio = serde_json::from_value(legacy).expect("legacy ratio");
+        assert_eq!(ratio.tokens_saved, 60);
+        assert!(ratio.savings_eligible);
+        assert_eq!(ratio.savings_status, "legacy_mechanical_reduction");
+    }
+
+    #[test]
+    fn receipt_no_match_fails_closed_and_claims_zero_savings() {
+        let index = ingest_documents(docs(), 80, 16);
+        let receipt = build_receipt(
+            &index,
+            "quantum chromodynamics lattice gauge calibration",
+            60,
+        )
+        .expect("receipt");
+
+        assert!(receipt.selected_context.is_empty());
+        assert_eq!(receipt.compression_ratio.tokens_saved, 0);
+        assert_eq!(
+            receipt.compression_ratio.tokens_withheld,
+            receipt.compression_ratio.source_tokens
+        );
+        assert!(!receipt.compression_ratio.savings_eligible);
+        assert_eq!(
+            receipt.compression_ratio.savings_status,
+            "not_credited_no_relevance_evidence"
+        );
+        assert!(receipt
+            .warnings
+            .iter()
+            .any(|warning| warning.contains("selection failed closed")));
+    }
+
+    #[test]
+    fn receipt_matched_selection_reports_only_mechanical_unverified_savings() {
+        let index = ingest_documents(docs(), 80, 16);
+        let receipt = build_receipt(&index, "change of control merger", 20).expect("receipt");
+
+        assert!(!receipt.selected_context.is_empty());
+        assert!(receipt.compression_ratio.savings_eligible);
+        assert_eq!(
+            receipt.compression_ratio.tokens_saved,
+            receipt.compression_ratio.tokens_withheld
+        );
+        assert_eq!(
+            receipt.compression_ratio.savings_status,
+            "mechanical_reduction_unverified"
