---
claim_id: c4e19995-f219-4c00-b526-6fba916dd2a0
entity: bm25
status: inferred
confidence: 0.75
sources:
  - entroly-core\src\bm25.rs:34
  - entroly-core\src\bm25.rs:45
  - entroly-core\src\bm25.rs:244
  - entroly-core\src\bm25.rs:281
  - entroly-core\src\bm25.rs:296
  - entroly-core\src\bm25.rs:334
  - entroly-core\src\bm25.rs:360
last_checked: 2026-04-23T03:07:07.833481+00:00
derived_from:
  - belief_compiler
  - sast
---

# Module: bm25

**Language:** rust
**Lines of code:** 453

## Types
- `pub struct BM25Index` — Pre-computed corpus statistics for BM25 IDF calculation.
- `pub struct BM25Score` — Per-document BM25 score breakdown (for explainability).

## Functions
- `fn tokenize_code(content: &str) -> Vec<String>` — Tokenize code content into lowercase terms.  Splits on non-alphanumeric boundaries, handles camelCase and snake_case, filters terms < 2 chars and common language keywords.
- `fn tokenize_path(path: &str) -> Vec<String>` — Tokenize a file path into meaningful terms.  "libs/langgraph/graph/state.py" → ["libs", "langgraph", "graph", "state"]
- `fn split_identifier(id: &str) -> Vec<String>` — Split a camelCase or PascalCase identifier into lowercase parts.  "StateGraph" → ["state", "graph"] "add_conditional_edges" → ["add", "conditional", "edges"] "HTMLParser" → ["html", "parser"]
- `fn is_code_stopword(word: &str) -> bool` — Common programming keywords that don't help with retrieval.
- `fn normalize_scores(scores: &[f64]) -> Vec<f64>` — Normalize BM25 scores to [0.0, 1.0] range for integration with existing PRISM scoring.  Uses min-max normalization with a floor to prevent zero scores: normalized = (score - min) / (max - min)  clampe

## Dependencies
- `std::collections::HashMap`
