[build-system]
requires = ["hatchling>=1.27,<1.31"]
build-backend = "hatchling.build"

[project]
name = "entroly"
version = "1.0.79"

description = "Drop-in Context Assurance for AI agents: reduce avoidable token usage with evidence selection, receipts, exact recovery, and verification."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [
    { name = "entroly", email = "fastrunner10090@gmail.com" },
]
keywords = [
    "ai-agents", "ai-coding-agents", "ai-cost-optimization", "llm-cost-optimization",
    "reduce-ai-costs", "context-assurance", "context-engineering", "context-management",
    "context-optimization", "context-compression", "prompt-compression", "token-optimization",
    "token-reduction", "context-window", "context-receipts", "content-addressed-recovery",
    "mcp", "mcp-server", "model-context-protocol", "github-copilot", "cursor", "claude-code",
    "codex", "openclaw", "hermes-agent", "opencode", "local-models", "agent-memory",
    "llm", "hallucination-detection",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "mcp>=1.28.1,<2",
    "httpx>=0.28.1",
    "starlette>=1.3.1",
    "uvicorn>=0.51.0",
    # Required, not optional -- see the root pyproject.toml for the measurement.
    # Query-conditioned selection (QCCR) is gated on this module, so an install
    # without it fills the token budget without ever reading the query and
    # reports an identical "saving" for every possible question.
    "entroly-core>=1.0.79,<2",
]

[project.optional-dependencies]
proxy = [
    "httpx>=0.28.1",
    "starlette>=1.3.1",
    "uvicorn>=0.51.0",
]
# Compatibility alias: the native engine now ships with the base install, so
# `entroly[native]` and a plain `entroly` resolve to the same thing.
native = [
    "entroly-core>=1.0.79,<2",
]
receipt-proof = [
    "cryptography>=42",
]
code-intelligence = [
    "tree-sitter-language-pack>=1.14.3,<2",
]
images = [
    "Pillow>=10,<13",
]
full = [
    "cryptography>=42",
    "entroly-core>=1.0.79,<2",
    "httpx>=0.28.1",
    "starlette>=1.3.1",
    "uvicorn>=0.51.0",
    "tree-sitter-language-pack>=1.14.3,<2",
]
test = [
    "pytest>=8,<9",
    "pytest-timeout>=2.3,<3",
]

[project.urls]
Homepage = "https://juyterman1000.github.io/entroly/docs/index.html"
Documentation = "https://juyterman1000.github.io/entroly/docs/index.html"
Source = "https://github.com/juyterman1000/entroly"
Issues = "https://github.com/juyterman1000/entroly/issues"
"Release Notes" = "https://github.com/juyterman1000/entroly/releases"

[project.scripts]
entroly = "entroly.docker_launcher_safe:launch"
entroly-memory = "entroly.memory_cli:main"
entroly-compression-mcp = "entroly.compression_mcp:main"
entroly-repository-mcp = "entroly.repository_intelligence.mcp:main"

[tool.hatch.build.targets.wheel]
packages = ["entroly"]
