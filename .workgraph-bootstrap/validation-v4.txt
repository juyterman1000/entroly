== rustfmt targeted ==
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/simhash_wide.rs:124:
 
     fn doc(seed: u64, words: usize) -> String {
         let vocab = [
-            "handler", "request", "timeout", "retry", "payment", "gateway", "session",
-            "token", "database", "encode", "decode", "commit", "buffer", "segment",
-            "manifest", "digest", "cursor", "batch",
+            "handler", "request", "timeout", "retry", "payment", "gateway", "session", "token",
+            "database", "encode", "decode", "commit", "buffer", "segment", "manifest", "digest",
+            "cursor", "batch",
         ];
         let mut state = seed.wrapping_mul(6364136223846793005).wrapping_add(1);
         (0..words)
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/simhash_wide.rs:226:
         // overlap, while both production-width options are separable. Sampling
         // noise does not guarantee 1024's finite fixture margin is strictly
         // greater than 256's, so monotonic fixture margins are not a valid test.
-        assert!(margins[0] < 0.0, "64-bit fixture should expose overlap: {margins:?}");
+        assert!(
+            margins[0] < 0.0,
+            "64-bit fixture should expose overlap: {margins:?}"
+        );
         assert!(
             margins[1] > 0.0 && margins[2] > 0.0,
             "256/1024-bit fixtures must separate populations: {margins:?}"
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:54:
             Self::InvalidInput(msg) => write!(f, "invalid work-graph input: {msg}"),
             Self::LimitExceeded(msg) => write!(f, "work-graph limit exceeded: {msg}"),
             Self::RepoMismatch { expected, actual } => {
-                write!(f, "work-graph repo mismatch: expected {expected:?}, got {actual:?}")
+                write!(
+                    f,
+                    "work-graph repo mismatch: expected {expected:?}, got {actual:?}"
+                )
             }
             Self::IntegrityMismatch { expected, actual } => write!(
                 f,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:204:
     }
 }
 
-#[derive(Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
+#[derive(
+    Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq, PartialOrd, Ord, Hash, Default,
+)]
 #[serde(rename_all = "snake_case")]
 pub enum WorkStatus {
     #[default]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:244:
     Other,
 }
 
-
 #[derive(Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq, Hash)]
 #[serde(rename_all = "snake_case")]
 pub enum VerificationState {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:350:
 #[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
 #[serde(tag = "op", rename_all = "snake_case")]
 pub enum WorkOperation {
-    AddEvidence { evidence: EvidenceRef },
-    UpsertNode { node: WorkNode },
-    UpsertEdge { edge: WorkEdge },
+    AddEvidence {
+        evidence: EvidenceRef,
+    },
+    UpsertNode {
+        node: WorkNode,
+    },
+    UpsertEdge {
+        edge: WorkEdge,
+    },
     SetStatus {
         node_id: String,
         status: WorkStatus,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:748:
         self.events.push(event.clone());
 
         let result = if append_in_order {
-            self.apply_materialized(&event).and_then(|_| self.refresh_commitment())
+            self.apply_materialized(&event)
+                .and_then(|_| self.refresh_commitment())
         } else {
             self.rebuild()
         };
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:948:
         }
     }
 
-    pub fn resume(&self, workstream_id: Option<&str>, max_evidence: usize) -> Result<ResumeView, WorkGraphError> {
+    pub fn resume(
+        &self,
+        workstream_id: Option<&str>,
+        max_evidence: usize,
+    ) -> Result<ResumeView, WorkGraphError> {
         let selected = if let Some(id) = workstream_id.filter(|id| !id.is_empty()) {
             let node = self
                 .nodes
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:961:
             }
             self.work_item_view(node)
         } else {
-            self.unfinished_work()
-                .into_iter()
-                .next()
-                .ok_or_else(|| WorkGraphError::UnknownNode("no unfinished workstream".to_string()))?
+            self.unfinished_work().into_iter().next().ok_or_else(|| {
+                WorkGraphError::UnknownNode("no unfinished workstream".to_string())
+            })?
         };
 
         let related = self.connected_node_ids(&selected.node_id, 2, 10_000);
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1045:
             symbols: Vec<String>,
         }
         let mut leases = Vec::new();
-        for node in self.nodes.values().filter(|n| n.kind == NodeKind::WorkLease) {
+        for node in self
+            .nodes
+            .values()
+            .filter(|n| n.kind == NodeKind::WorkLease)
+        {
             let expires = attr_i64(&node.attributes, "expires_at_ms").unwrap_or(0);
             if expires <= now_ms {
                 continue;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1218:
             return Ok(false);
         };
         if workstream.kind != NodeKind::Workstream
-            || receipt.node_ids.binary_search(&receipt.workstream_id).is_err()
+            || receipt
+                .node_ids
+                .binary_search(&receipt.workstream_id)
+                .is_err()
         {
             return Ok(false);
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1225:
         if receipt.node_ids.windows(2).any(|pair| pair[0] >= pair[1])
             || receipt.edge_ids.windows(2).any(|pair| pair[0] >= pair[1])
-            || receipt.evidence_ids.windows(2).any(|pair| pair[0] >= pair[1])
+            || receipt
+                .evidence_ids
+                .windows(2)
+                .any(|pair| pair[0] >= pair[1])
         {
             return Ok(false);
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1231:
-        if receipt.node_ids.iter().any(|id| !self.nodes.contains_key(id))
-            || receipt.edge_ids.iter().any(|id| !self.edges.contains_key(id))
+        if receipt
+            .node_ids
+            .iter()
+            .any(|id| !self.nodes.contains_key(id))
             || receipt
+                .edge_ids
+                .iter()
+                .any(|id| !self.edges.contains_key(id))
+            || receipt
                 .evidence_ids
                 .iter()
                 .any(|id| !self.evidence.contains_key(id))
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1274:
     }
 
     fn rebuild(&mut self) -> Result<(), WorkGraphError> {
-        self.events
-            .sort_by(|a, b| a.observed_at_ms.cmp(&b.observed_at_ms).then_with(|| a.event_id.cmp(&b.event_id)));
+        self.events.sort_by(|a, b| {
+            a.observed_at_ms
+                .cmp(&b.observed_at_ms)
+                .then_with(|| a.event_id.cmp(&b.event_id))
+        });
         self.nodes.clear();
         self.edges.clear();
         self.evidence.clear();
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1316:
                     Some(existing) => {
                         let may_overwrite = incoming.trust >= existing.trust;
                         existing.trust = existing.trust.max(incoming.trust);
-                        existing.observed_at_ms = existing.observed_at_ms.max(incoming.observed_at_ms);
+                        existing.observed_at_ms =
+                            existing.observed_at_ms.max(incoming.observed_at_ms);
                         if may_overwrite {
                             for (key, value) in incoming.attributes {
                                 existing.attributes.insert(key, value);
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1384:
                         self.edges.insert(incoming.edge_id.clone(), incoming);
                     }
                 }
-                self.adjacency.entry(from.clone()).or_default().insert(to.clone());
+                self.adjacency
+                    .entry(from.clone())
+                    .or_default()
+                    .insert(to.clone());
                 self.adjacency.entry(to).or_default().insert(from);
             }
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1409:
                         node.status = *status;
                         node.status_trust = *trust;
                         if !reason.is_empty() {
-                            node.attributes.insert(
-                                "status_reason".to_string(),
-                                Value::String(reason.clone()),
-                            );
+                            node.attributes
+                                .insert("status_reason".to_string(), Value::String(reason.clone()));
                         }
                     }
                 }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1645:
         let mut verification_ids = BTreeSet::new();
         let mut evidence_ids = node.evidence_ids.clone();
         for id in &related {
-            let Some(n) = self.nodes.get(id) else { continue };
+            let Some(n) = self.nodes.get(id) else {
+                continue;
+            };
             evidence_ids.extend(n.evidence_ids.iter().cloned());
             match n.kind {
                 NodeKind::Task => {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1693:
     }
 }
 
-fn observation_to_event(repo_id: &str, mut obs: RepositoryObservation) -> Result<WorkEvent, WorkGraphError> {
+fn observation_to_event(
+    repo_id: &str,
+    mut obs: RepositoryObservation,
+) -> Result<WorkEvent, WorkGraphError> {
     validate_text("repo_id", &obs.repo_id, MAX_ID_LEN, false)?;
-    validate_text("repository_label", &obs.repository_label, MAX_LABEL_LEN, true)?;
+    validate_text(
+        "repository_label",
+        &obs.repository_label,
+        MAX_LABEL_LEN,
+        true,
+    )?;
     validate_text("agent_id", &obs.agent_id, MAX_ID_LEN, true)?;
     validate_text("session_id", &obs.session_id, MAX_ID_LEN, true)?;
     if obs.changes.len() > MAX_OPERATIONS_PER_EVENT / 2 {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1702:
-        return Err(WorkGraphError::LimitExceeded("too many file changes in one observation".to_string()));
+        return Err(WorkGraphError::LimitExceeded(
+            "too many file changes in one observation".to_string(),
+        ));
     }
     if obs.leases.len() > MAX_SCOPE_ITEMS {
-        return Err(WorkGraphError::LimitExceeded("too many work leases".to_string()));
+        return Err(WorkGraphError::LimitExceeded(
+            "too many work leases".to_string(),
+        ));
     }
 
     // Adapters discover filesystem/Git/provider facts in different orders.
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1804:
         repo_attrs.insert("branch".to_string(), Value::String(obs.branch.name.clone()));
     }
     if !obs.branch.head_sha.is_empty() {
-        repo_attrs.insert("head_sha".to_string(), Value::String(obs.branch.head_sha.clone()));
+        repo_attrs.insert(
+            "head_sha".to_string(),
+            Value::String(obs.branch.head_sha.clone()),
+        );
     }
     operations.push(WorkOperation::UpsertNode {
         node: WorkNode {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1850:
         });
     }
 
-    let (task_id, task_label, task_trust, task_explicit_status, remaining_work, task_source_kind, task_source) =
-        if let Some(task) = obs.task_hint.take() {
-            validate_text("task title", &task.title, MAX_LABEL_LEN, false)?;
-            let task_id = if task.task_id.is_empty() {
-                stable_node_id(NodeKind::Task, repo_id, &task.title)
-            } else {
-                validate_text("task_id", &task.task_id, MAX_ID_LEN, false)?;
-                stable_node_id(NodeKind::Task, repo_id, &task.task_id)
-            };
-            (
-                task_id,
-                task.title,
-                task.trust,
-                task.explicit_status,
-                task.remaining_work,
-                task.source_kind,
-                task.source_ref,
-            )
+    let (
+        task_id,
+        task_label,
+        task_trust,
+        task_explicit_status,
+        remaining_work,
+        task_source_kind,
+        task_source,
+    ) = if let Some(task) = obs.task_hint.take() {
+        validate_text("task title", &task.title, MAX_LABEL_LEN, false)?;
+        let task_id = if task.task_id.is_empty() {
+            stable_node_id(NodeKind::Task, repo_id, &task.title)
         } else {
-            let branch_label = if !obs.branch.name.is_empty() {
-                obs.branch.name.clone()
-            } else {
-                "detached-head".to_string()
-            };
-            let label = if meaningful_git {
-                format!("Repository work on {branch_label}")
-            } else {
-                "Repository work".to_string()
-            };
-            (
-                stable_node_id(NodeKind::Task, repo_id, &format!("inferred:{branch_label}")),
-                label,
-                TrustLevel::Inferred,
-                WorkStatus::Unknown,
-                Vec::new(),
-                EvidenceKind::RepositoryFact,
-                "inferred:repository-state".to_string(),
-            )
+            validate_text("task_id", &task.task_id, MAX_ID_LEN, false)?;
+            stable_node_id(NodeKind::Task, repo_id, &task.task_id)
         };
+        (
+            task_id,
+            task.title,
+            task.trust,
+            task.explicit_status,
+            task.remaining_work,
+            task.source_kind,
+            task.source_ref,
+        )
+    } else {
+        let branch_label = if !obs.branch.name.is_empty() {
+            obs.branch.name.clone()
+        } else {
+            "detached-head".to_string()
+        };
+        let label = if meaningful_git {
+            format!("Repository work on {branch_label}")
+        } else {
+            "Repository work".to_string()
+        };
+        (
+            stable_node_id(NodeKind::Task, repo_id, &format!("inferred:{branch_label}")),
+            label,
+            TrustLevel::Inferred,
+            WorkStatus::Unknown,
+            Vec::new(),
+            EvidenceKind::RepositoryFact,
+            "inferred:repository-state".to_string(),
+        )
+    };
 
     let workstream_key = format!("{}:{}", obs.branch.name, task_id);
     let workstream_id = stable_node_id(NodeKind::Workstream, repo_id, &workstream_key);
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1932:
 
     let mut workstream_attrs = BTreeMap::new();
     workstream_attrs.insert("branch".to_string(), Value::String(obs.branch.name.clone()));
-    workstream_attrs.insert("head_sha".to_string(), Value::String(obs.branch.head_sha.clone()));
-    workstream_attrs.insert("base_ref".to_string(), Value::String(obs.branch.base_ref.clone()));
+    workstream_attrs.insert(
+        "head_sha".to_string(),
+        Value::String(obs.branch.head_sha.clone()),
+    );
+    workstream_attrs.insert(
+        "base_ref".to_string(),
+        Value::String(obs.branch.base_ref.clone()),
+    );
     workstream_attrs.insert("ahead_by".to_string(), Value::from(obs.branch.ahead_by));
     workstream_attrs.insert("behind_by".to_string(), Value::from(obs.branch.behind_by));
     workstream_attrs.insert(
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1949:
             status: WorkStatus::Unknown,
             status_trust: TrustLevel::Untrusted,
             attributes: workstream_attrs,
-            evidence_ids: BTreeSet::from([repo_evidence.evidence_id.clone(), task_evidence.evidence_id.clone()]),
+            evidence_ids: BTreeSet::from([
+                repo_evidence.evidence_id.clone(),
+                task_evidence.evidence_id.clone(),
+            ]),
             updated_at_ms: obs.observed_at_ms,
         },
     });
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1999:
     if !obs.session_id.is_empty() {
         let session_id = stable_node_id(NodeKind::Session, repo_id, &obs.session_id);
         let mut attrs = BTreeMap::new();
-        attrs.insert("session_id".to_string(), Value::String(obs.session_id.clone()));
+        attrs.insert(
+            "session_id".to_string(),
+            Value::String(obs.session_id.clone()),
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: session_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2034:
     }
 
     let mut blocked = obs.branch.merge_in_progress || obs.branch.rebase_in_progress;
-    let mut blocked_trust = if blocked { TrustLevel::Observed } else { TrustLevel::Untrusted };
+    let mut blocked_trust = if blocked {
+        TrustLevel::Observed
+    } else {
+        TrustLevel::Untrusted
+    };
     let mut has_passing_verification = false;
     let mut status_evidence_ids = BTreeSet::from([
         repo_evidence.evidence_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2049:
         let source_ref = format!("git-status:{}", obs.branch.head_sha);
         let mut ev_attrs = BTreeMap::new();
         ev_attrs.insert("path".to_string(), Value::String(change.path.clone()));
-        ev_attrs.insert("change_kind".to_string(), serde_json::to_value(change.kind)?);
+        ev_attrs.insert(
+            "change_kind".to_string(),
+            serde_json::to_value(change.kind)?,
+        );
         let evidence = with_evidence_id(EvidenceRef {
             evidence_id: String::new(),
             kind: EvidenceKind::GitStatus,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2066:
         let file_id = stable_node_id(NodeKind::File, repo_id, &change.path);
         let mut file_attrs = BTreeMap::new();
         file_attrs.insert("path".to_string(), Value::String(change.path.clone()));
-        file_attrs.insert("change_kind".to_string(), serde_json::to_value(change.kind)?);
+        file_attrs.insert(
+            "change_kind".to_string(),
+            serde_json::to_value(change.kind)?,
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: file_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2087:
         change_attrs.insert("staged".to_string(), Value::Bool(change.staged));
         change_attrs.insert("conflicted".to_string(), Value::Bool(change.conflicted));
         if !change.old_path.is_empty() {
-            change_attrs.insert("old_path".to_string(), Value::String(change.old_path.clone()));
+            change_attrs.insert(
+                "old_path".to_string(),
+                Value::String(change.old_path.clone()),
+            );
         }
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2122:
             blocked = true;
             blocked_trust = blocked_trust.max(TrustLevel::Observed);
             status_evidence_ids.insert(evidence.evidence_id.clone());
-            let failure_id = stable_node_id(NodeKind::Failure, repo_id, &format!("conflict:{}", change.path));
+            let failure_id = stable_node_id(
+                NodeKind::Failure,
+                repo_id,
+                &format!("conflict:{}", change.path),
+            );
             operations.push(WorkOperation::UpsertNode {
                 node: WorkNode {
                     node_id: failure_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2156:
             digest: commit.sha.clone(),
             locator: String::new(),
             trust: TrustLevel::Observed,
-            observed_at_ms: if commit.timestamp_ms == 0 { obs.observed_at_ms } else { commit.timestamp_ms },
+            observed_at_ms: if commit.timestamp_ms == 0 {
+                obs.observed_at_ms
+            } else {
+                commit.timestamp_ms
+            },
             attributes: BTreeMap::new(),
         })?;
-        operations.push(WorkOperation::AddEvidence { evidence: evidence.clone() });
+        operations.push(WorkOperation::AddEvidence {
+            evidence: evidence.clone(),
+        });
         let commit_id = stable_node_id(NodeKind::Commit, repo_id, &commit.sha);
         let mut attrs = BTreeMap::new();
         attrs.insert("sha".to_string(), Value::String(commit.sha.clone()));
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2166:
-        attrs.insert("subject".to_string(), Value::String(bounded(&commit.subject, MAX_LABEL_LEN)));
-        attrs.insert("changed_paths".to_string(), serde_json::to_value(&commit.changed_paths)?);
+        attrs.insert(
+            "subject".to_string(),
+            Value::String(bounded(&commit.subject, MAX_LABEL_LEN)),
+        );
+        attrs.insert(
+            "changed_paths".to_string(),
+            serde_json::to_value(&commit.changed_paths)?,
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: commit_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2171:
                 kind: NodeKind::Commit,
-                label: if commit.subject.is_empty() { commit.sha.clone() } else { bounded(&commit.subject, MAX_LABEL_LEN) },
+                label: if commit.subject.is_empty() {
+                    commit.sha.clone()
+                } else {
+                    bounded(&commit.subject, MAX_LABEL_LEN)
+                },
                 trust: TrustLevel::Observed,
                 status: WorkStatus::Unknown,
                 status_trust: TrustLevel::Untrusted,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2178:
                 updated_at_ms: obs.observed_at_ms,
             },
         });
-        operations.push(edge_op(&commit_id, &workstream_id, EdgeKind::PartOf, TrustLevel::Observed, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+        operations.push(edge_op(
+            &commit_id,
+            &workstream_id,
+            EdgeKind::PartOf,
+            TrustLevel::Observed,
+            obs.observed_at_ms,
+            std::slice::from_ref(&evidence.evidence_id),
+        ));
         if !agent_node_id.is_empty() {
-            operations.push(edge_op(&commit_id, &agent_node_id, EdgeKind::ProducedBy, TrustLevel::Observed, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+            operations.push(edge_op(
+                &commit_id,
+                &agent_node_id,
+                EdgeKind::ProducedBy,
+                TrustLevel::Observed,
+                obs.observed_at_ms,
+                std::slice::from_ref(&evidence.evidence_id),
+            ));
         }
         for path in &commit.changed_paths {
             if validate_path(path).is_err() {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2202:
                     updated_at_ms: obs.observed_at_ms,
                 },
             });
-            operations.push(edge_op(&commit_id, &file_id, EdgeKind::Changed, TrustLevel::Observed, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+            operations.push(edge_op(
+                &commit_id,
+                &file_id,
+                EdgeKind::Changed,
+                TrustLevel::Observed,
+                obs.observed_at_ms,
+                std::slice::from_ref(&evidence.evidence_id),
+            ));
         }
     }
 
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2209:
     for verification in &obs.verifications {
         let ev_kind = match verification.evidence_kind {
-            EvidenceKind::TestResult | EvidenceKind::CiResult | EvidenceKind::RavsOutcome => verification.evidence_kind,
+            EvidenceKind::TestResult | EvidenceKind::CiResult | EvidenceKind::RavsOutcome => {
+                verification.evidence_kind
+            }
             _ => EvidenceKind::TestResult,
         };
         let evidence = with_evidence_id(EvidenceRef {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2218:
             digest: verification.digest.clone(),
             locator: verification.name.clone(),
             trust: TrustLevel::Verified,
-            observed_at_ms: if verification.observed_at_ms == 0 { obs.observed_at_ms } else { verification.observed_at_ms },
+            observed_at_ms: if verification.observed_at_ms == 0 {
+                obs.observed_at_ms
+            } else {
+                verification.observed_at_ms
+            },
             attributes: BTreeMap::new(),
         })?;
-        operations.push(WorkOperation::AddEvidence { evidence: evidence.clone() });
-        let kind = if ev_kind == EvidenceKind::CiResult { NodeKind::CiRun } else { NodeKind::Test };
+        operations.push(WorkOperation::AddEvidence {
+            evidence: evidence.clone(),
+        });
+        let kind = if ev_kind == EvidenceKind::CiResult {
+            NodeKind::CiRun
+        } else {
+            NodeKind::Test
+        };
         let verification_key = if verification.verification_id.is_empty() {
             format!("{}:{}", verification.name, verification.source_ref)
         } else {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2230:
         };
         let verification_id = stable_node_id(kind, repo_id, &verification_key);
         let mut attrs = BTreeMap::new();
-        attrs.insert("verification_state".to_string(), serde_json::to_value(verification.state)?);
+        attrs.insert(
+            "verification_state".to_string(),
+            serde_json::to_value(verification.state)?,
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: verification_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2242:
                     VerificationState::Failed => WorkStatus::Blocked,
                     _ => WorkStatus::Unknown,
                 },
-                status_trust: if matches!(verification.state, VerificationState::Passed | VerificationState::Failed) {
+                status_trust: if matches!(
+                    verification.state,
+                    VerificationState::Passed | VerificationState::Failed
+                ) {
                     TrustLevel::Verified
                 } else {
                     TrustLevel::Untrusted
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2252:
                 updated_at_ms: obs.observed_at_ms,
             },
         });
-        operations.push(edge_op(&workstream_id, &verification_id, EdgeKind::VerifiedBy, TrustLevel::Verified, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+        operations.push(edge_op(
+            &workstream_id,
+            &verification_id,
+            EdgeKind::VerifiedBy,
+            TrustLevel::Verified,
+            obs.observed_at_ms,
+            std::slice::from_ref(&evidence.evidence_id),
+        ));
         match verification.state {
             VerificationState::Passed => {
                 has_passing_verification = true;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2262:
                 status_evidence_ids.insert(evidence.evidence_id.clone());
                 blocked = true;
                 blocked_trust = TrustLevel::Verified;
-                let failure_id = stable_node_id(NodeKind::Failure, repo_id, &format!("verification:{verification_id}"));
+                let failure_id = stable_node_id(
+                    NodeKind::Failure,
+                    repo_id,
+                    &format!("verification:{verification_id}"),
+                );
                 operations.push(WorkOperation::UpsertNode {
                     node: WorkNode {
                         node_id: failure_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2276:
                         updated_at_ms: obs.observed_at_ms,
                     },
                 });
-                operations.push(edge_op(&failure_id, &workstream_id, EdgeKind::Blocks, TrustLevel::Verified, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+                operations.push(edge_op(
+                    &failure_id,
+                    &workstream_id,
+                    EdgeKind::Blocks,
+                    TrustLevel::Verified,
+                    obs.observed_at_ms,
+                    std::slice::from_ref(&evidence.evidence_id),
+                ));
             }
             _ => {}
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2294:
             observed_at_ms: obs.observed_at_ms,
             attributes: BTreeMap::new(),
         })?;
-        operations.push(WorkOperation::AddEvidence { evidence: evidence.clone() });
+        operations.push(WorkOperation::AddEvidence {
+            evidence: evidence.clone(),
+        });
         let decision_key = if decision.decision_id.is_empty() {
             format!("{task_id}:{}", decision.text)
         } else {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2314:
                 updated_at_ms: obs.observed_at_ms,
             },
         });
-        operations.push(edge_op(&decision_id, &workstream_id, EdgeKind::PartOf, decision.trust, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+        operations.push(edge_op(
+            &decision_id,
+            &workstream_id,
+            EdgeKind::PartOf,
+            decision.trust,
+            obs.observed_at_ms,
+            std::slice::from_ref(&evidence.evidence_id),
+        ));
     }
 
     for claim in &obs.claims {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2321:
         validate_text("claim", &claim.text, MAX_LABEL_LEN, false)?;
-        let risk = if claim.risk.is_finite() { claim.risk.clamp(0.0, 1.0) } else { 1.0 };
+        let risk = if claim.risk.is_finite() {
+            claim.risk.clamp(0.0, 1.0)
+        } else {
+            1.0
+        };
         // Claim state never manufactures trust. The adapter must explicitly
         // state how the assessment was established (for example WITNESS/RAVS).
         // Raw agent/user statements therefore remain untrusted by default.
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2331:
         };
         let claim_id = stable_node_id(NodeKind::Claim, repo_id, &claim_key);
         let mut attrs = BTreeMap::new();
-        attrs.insert("claim_state".to_string(), serde_json::to_value(claim.state)?);
+        attrs.insert(
+            "claim_state".to_string(),
+            serde_json::to_value(claim.state)?,
+        );
         attrs.insert("risk".to_string(), Value::from(risk));
-        attrs.insert("source_ref".to_string(), Value::String(claim.source_ref.clone()));
+        attrs.insert(
+            "source_ref".to_string(),
+            Value::String(claim.source_ref.clone()),
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: claim_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2340:
                 kind: NodeKind::Claim,
                 label: claim.text.clone(),
                 trust,
-                status: if claim.state == ClaimState::Contradicted { WorkStatus::Blocked } else { WorkStatus::Unknown },
-                status_trust: if claim.state == ClaimState::Contradicted { trust } else { TrustLevel::Untrusted },
+                status: if claim.state == ClaimState::Contradicted {
+                    WorkStatus::Blocked
+                } else {
+                    WorkStatus::Unknown
+                },
+                status_trust: if claim.state == ClaimState::Contradicted {
+                    trust
+                } else {
+                    TrustLevel::Untrusted
+                },
                 attributes: attrs,
                 evidence_ids: claim.evidence_ids.iter().cloned().collect(),
                 updated_at_ms: obs.observed_at_ms,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2348:
             },
         });
-        operations.push(edge_op(&claim_id, &workstream_id, EdgeKind::PartOf, trust, obs.observed_at_ms, &claim.evidence_ids));
+        operations.push(edge_op(
+            &claim_id,
+            &workstream_id,
+            EdgeKind::PartOf,
+            trust,
+            obs.observed_at_ms,
+            &claim.evidence_ids,
+        ));
         if claim.state == ClaimState::Contradicted {
-            let failure_id = stable_node_id(NodeKind::Failure, repo_id, &format!("claim:{claim_id}"));
+            let failure_id =
+                stable_node_id(NodeKind::Failure, repo_id, &format!("claim:{claim_id}"));
             operations.push(WorkOperation::UpsertNode {
                 node: WorkNode {
                     node_id: failure_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2363:
                     updated_at_ms: obs.observed_at_ms,
                 },
             });
-            operations.push(edge_op(&claim_id, &failure_id, EdgeKind::ContradictedBy, trust, obs.observed_at_ms, &claim.evidence_ids));
+            operations.push(edge_op(
+                &claim_id,
+                &failure_id,
+                EdgeKind::ContradictedBy,
+                trust,
+                obs.observed_at_ms,
+                &claim.evidence_ids,
+            ));
         }
     }
 
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2370:
     for lease in &obs.leases {
         validate_text("lease agent", &lease.agent_id, MAX_ID_LEN, false)?;
         if lease.scope_paths.len() + lease.scope_symbols.len() > MAX_SCOPE_ITEMS {
-            return Err(WorkGraphError::LimitExceeded("lease scope too large".to_string()));
+            return Err(WorkGraphError::LimitExceeded(
+                "lease scope too large".to_string(),
+            ));
         }
         for path in &lease.scope_paths {
             validate_path(path)?;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2407:
             attributes: BTreeMap::new(),
         })?;
         let lease_evidence_id = lease_evidence.evidence_id.clone();
-        operations.push(WorkOperation::AddEvidence { evidence: lease_evidence });
+        operations.push(WorkOperation::AddEvidence {
+            evidence: lease_evidence,
+        });
         let mut attrs = BTreeMap::new();
-        attrs.insert("agent_id".to_string(), Value::String(lease.agent_id.clone()));
         attrs.insert(
+            "agent_id".to_string(),
+            Value::String(lease.agent_id.clone()),
+        );
+        attrs.insert(
             "task_id".to_string(),
-            Value::String(if lease.task_id.is_empty() { task_id.clone() } else { lease.task_id.clone() }),
+            Value::String(if lease.task_id.is_empty() {
+                task_id.clone()
+            } else {
+                lease.task_id.clone()
+            }),
         );
-        attrs.insert("scope_paths".to_string(), serde_json::to_value(&lease.scope_paths)?);
-        attrs.insert("scope_symbols".to_string(), serde_json::to_value(&lease.scope_symbols)?);
-        attrs.insert("expires_at_ms".to_string(), Value::from(lease.expires_at_ms));
+        attrs.insert(
+            "scope_paths".to_string(),
+            serde_json::to_value(&lease.scope_paths)?,
+        );
+        attrs.insert(
+            "scope_symbols".to_string(),
+            serde_json::to_value(&lease.scope_symbols)?,
+        );
+        attrs.insert(
+            "expires_at_ms".to_string(),
+            Value::from(lease.expires_at_ms),
+        );
         attrs.insert("source_ref".to_string(), Value::String(lease_source_ref));
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2482:
             },
         });
         let execution_key = if execution.execution_id.is_empty() {
-            format!("{}:{}:{}:{}", task_id, execution.provider, execution.model, execution.source_ref)
+            format!(
+                "{}:{}:{}:{}",
+                task_id, execution.provider, execution.model, execution.source_ref
+            )
         } else {
             execution.execution_id.clone()
         };
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2510:
             attributes: BTreeMap::new(),
         })?;
         let execution_evidence_id = execution_evidence.evidence_id.clone();
-        operations.push(WorkOperation::AddEvidence { evidence: execution_evidence });
+        operations.push(WorkOperation::AddEvidence {
+            evidence: execution_evidence,
+        });
         let mut attrs = BTreeMap::new();
-        attrs.insert("provider".to_string(), Value::String(execution.provider.clone()));
+        attrs.insert(
+            "provider".to_string(),
+            Value::String(execution.provider.clone()),
+        );
         attrs.insert("model".to_string(), Value::String(execution.model.clone()));
         attrs.insert("latency_ms".to_string(), Value::from(execution.latency_ms));
-        attrs.insert("cost_micro_usd".to_string(), Value::from(execution.cost_micro_usd));
+        attrs.insert(
+            "cost_micro_usd".to_string(),
+            Value::from(execution.cost_micro_usd),
+        );
         if let Some(success) = execution.success {
             attrs.insert("success".to_string(), Value::Bool(success));
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2522:
-        attrs.insert("source_ref".to_string(), Value::String(execution_source_ref));
+        attrs.insert(
+            "source_ref".to_string(),
+            Value::String(execution_source_ref),
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: execution_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2531:
                     Some(false) => WorkStatus::Blocked,
                     None => WorkStatus::Unknown,
                 },
-                status_trust: if execution.success.is_some() { TrustLevel::Observed } else { TrustLevel::Untrusted },
+                status_trust: if execution.success.is_some() {
+                    TrustLevel::Observed
+                } else {
+                    TrustLevel::Untrusted
+                },
                 attributes: attrs,
                 evidence_ids: BTreeSet::from([execution_evidence_id.clone()]),
                 updated_at_ms: obs.observed_at_ms,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2575:
         }
     } else if task_explicit_status != WorkStatus::Unknown {
         task_explicit_status
-    } else if meaningful_git || !obs.decisions.is_empty() || !obs.leases.is_empty() || !obs.model_executions.is_empty() {
+    } else if meaningful_git
+        || !obs.decisions.is_empty()
+        || !obs.leases.is_empty()
+        || !obs.model_executions.is_empty()
+    {
         WorkStatus::InProgress
     } else if had_explicit_task {
         WorkStatus::Planned
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2585:
     let status_reason = match status {
         WorkStatus::Blocked => "durable conflict or failing verification observed",
         WorkStatus::Completed => "explicit completion with passing verification",
-        WorkStatus::NeedsVerification => "completion was stated but independent verification is missing",
+        WorkStatus::NeedsVerification => {
+            "completion was stated but independent verification is missing"
+        }
         WorkStatus::Planned => "explicit task observed before durable work changes",
         WorkStatus::InProgress => "durable work evidence observed",
         WorkStatus::Abandoned => "task explicitly marked abandoned",
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2736:
 
 fn validate_event(event: &mut WorkEvent) -> Result<(), WorkGraphError> {
     validate_text("event_id", &event.event_id, 128, true)?;
-    validate_text("event source_ref", &event.source_ref, MAX_SOURCE_REF_LEN, true)?;
+    validate_text(
+        "event source_ref",
+        &event.source_ref,
+        MAX_SOURCE_REF_LEN,
+        true,
+    )?;
     validate_text("event actor_id", &event.actor_id, MAX_ID_LEN, true)?;
     validate_text("event session_id", &event.session_id, MAX_ID_LEN, true)?;
     if event.operations.len() > MAX_OPERATIONS_PER_EVENT {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2791:
 
 fn validate_evidence(evidence: &mut EvidenceRef) -> Result<(), WorkGraphError> {
     validate_text("evidence_id", &evidence.evidence_id, MAX_ID_LEN, true)?;
-    validate_text("evidence source_ref", &evidence.source_ref, MAX_SOURCE_REF_LEN, true)?;
+    validate_text(
+        "evidence source_ref",
+        &evidence.source_ref,
+        MAX_SOURCE_REF_LEN,
+        true,
+    )?;
     validate_text("evidence digest", &evidence.digest, MAX_ID_LEN, true)?;
-    validate_text("evidence locator", &evidence.locator, MAX_SOURCE_REF_LEN, true)?;
+    validate_text(
+        "evidence locator",
+        &evidence.locator,
+        MAX_SOURCE_REF_LEN,
+        true,
+    )?;
     if evidence.trust > max_trust_for_evidence_kind(evidence.kind) {
         return Err(WorkGraphError::InvalidInput(format!(
             "evidence kind {:?} cannot directly assert trust {:?}",
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2850:
 
 fn validate_id_set(name: &str, values: &BTreeSet<String>) -> Result<(), WorkGraphError> {
     if values.len() > MAX_SCOPE_ITEMS {
-        return Err(WorkGraphError::LimitExceeded(format!("{name} has too many IDs")));
+        return Err(WorkGraphError::LimitExceeded(format!(
+            "{name} has too many IDs"
+        )));
     }
     for value in values {
         validate_text(name, value, MAX_ID_LEN, false)?;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2892:
     Ok(())
 }
 
-fn validate_text(name: &str, text: &str, max_len: usize, allow_empty: bool) -> Result<(), WorkGraphError> {
+fn validate_text(
+    name: &str,
+    text: &str,
+    max_len: usize,
+    allow_empty: bool,
+) -> Result<(), WorkGraphError> {
     if !allow_empty && text.is_empty() {
-        return Err(WorkGraphError::InvalidInput(format!("{name} must not be empty")));
+        return Err(WorkGraphError::InvalidInput(format!(
+            "{name} must not be empty"
+        )));
     }
     if text.len() > max_len {
         return Err(WorkGraphError::LimitExceeded(format!(
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2975:
             if paths_overlap(left, right) {
                 let left_n = normalize_scope_path(left);
                 let right_n = normalize_scope_path(right);
-                result.insert(if left_n.len() >= right_n.len() { left_n } else { right_n });
+                result.insert(if left_n.len() >= right_n.len() {
+                    left_n
+                } else {
+                    right_n
+                });
             }
         }
     }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2985:
 fn overlap_exact(a: &[String], b: &[String]) -> Vec<String> {
     let left: BTreeSet<&str> = a.iter().map(String::as_str).collect();
     let right: BTreeSet<&str> = b.iter().map(String::as_str).collect();
-    left.intersection(&right).map(|s| (*s).to_string()).collect()
+    left.intersection(&right)
+        .map(|s| (*s).to_string())
+        .collect()
 }
 
 #[cfg(test)]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3021:
         let mut graph = WorkGraph::new("repo-1").unwrap();
         graph.observe_repository(clean_observation()).unwrap();
         assert!(graph.unfinished_work().is_empty());
-        assert_eq!(graph.nodes.len(), 1, "clean observation should create only repository node");
+        assert_eq!(
+            graph.nodes.len(),
+            1,
+            "clean observation should create only repository node"
+        );
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3041:
         let unfinished = graph.unfinished_work();
         assert_eq!(unfinished.len(), 1);
         assert_eq!(unfinished[0].status, WorkStatus::Planned);
-        assert!(graph.evidence.values().any(|e| e.kind == EvidenceKind::Checkpoint));
+        assert!(graph
+            .evidence
+            .values()
+            .any(|e| e.kind == EvidenceKind::Checkpoint));
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3062:
         let unfinished = graph.unfinished_work();
         assert_eq!(unfinished.len(), 1);
         assert_eq!(unfinished[0].status, WorkStatus::InProgress);
-        assert!(unfinished[0].changed_paths.contains(&"src/auth.rs".to_string()));
+        assert!(unfinished[0]
+            .changed_paths
+            .contains(&"src/auth.rs".to_string()));
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3079:
             source_ref: "checkpoint:1".to_string(),
         });
         graph.observe_repository(obs.clone()).unwrap();
-        assert_eq!(graph.unfinished_work()[0].status, WorkStatus::NeedsVerification);
+        assert_eq!(
+            graph.unfinished_work()[0].status,
+            WorkStatus::NeedsVerification
+        );
 
         obs.observed_at_ms += 1;
         obs.verifications.push(VerificationObservation {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3093:
         });
         graph.observe_repository(obs).unwrap();
         assert!(graph.unfinished_work().is_empty());
-        assert!(graph.nodes.values().any(|n| n.kind == NodeKind::Task && n.status == WorkStatus::Completed));
+        assert!(graph
+            .nodes
+            .values()
+            .any(|n| n.kind == NodeKind::Task && n.status == WorkStatus::Completed));
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3147:
             evidence_ids: vec![],
         });
         graph.observe_repository(obs).unwrap();
-        let claim = graph.nodes.values().find(|n| n.kind == NodeKind::Claim).unwrap();
+        let claim = graph
+            .nodes
+            .values()
+            .find(|n| n.kind == NodeKind::Claim)
+            .unwrap();
         assert_eq!(claim.trust, TrustLevel::Untrusted);
         assert_eq!(claim.status, WorkStatus::Blocked);
     }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3189:
         let report = graph.coordination_report(2_000);
         assert_eq!(report.active_leases, 2);
         assert_eq!(report.conflicts.len(), 1);
-        assert_eq!(report.conflicts[0].overlapping_symbols, vec!["Auth.refresh"]);
+        assert_eq!(
+            report.conflicts[0].overlapping_symbols,
+            vec!["Auth.refresh"]
+        );
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3213:
         a.observe_repository(one.clone()).unwrap();
         b.observe_repository(two.clone()).unwrap();
         a.merge(&b).unwrap();
-        b.merge(&WorkGraph::from_json(&a.export_json(false).unwrap()).unwrap()).unwrap();
+        b.merge(&WorkGraph::from_json(&a.export_json(false).unwrap()).unwrap())
+            .unwrap();
         assert_eq!(a.graph_commitment(), b.graph_commitment());
         assert_eq!(a.event_count(), 2);
     }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3257:
         });
         graph.observe_repository(obs).unwrap();
         let id = graph.unfinished_work()[0].node_id.clone();
-        let mut receipt = graph.handoff_receipt(&id, "claude", "codex", 2_000).unwrap();
+        let mut receipt = graph
+            .handoff_receipt(&id, "claude", "codex", 2_000)
+            .unwrap();
         assert!(WorkGraph::verify_handoff_receipt(&receipt).unwrap());
         receipt.to_agent = "other".to_string();
         assert!(!WorkGraph::verify_handoff_receipt(&receipt).unwrap());
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3322:
             .node_id
             .clone();
         assert_eq!(graph.nodes[&workstream_id].status, WorkStatus::Completed);
-        assert_eq!(graph.nodes[&workstream_id].status_trust, TrustLevel::Verified);
+        assert_eq!(
+            graph.nodes[&workstream_id].status_trust,
+            TrustLevel::Verified
+        );
 
         let mut stale = clean_observation();
         stale.observed_at_ms += 1;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3337:
         });
         graph.observe_repository(stale).unwrap();
         assert_eq!(graph.nodes[&workstream_id].status, WorkStatus::Completed);
-        assert_eq!(graph.nodes[&workstream_id].status_trust, TrustLevel::Verified);
+        assert_eq!(
+            graph.nodes[&workstream_id].status_trust,
+            TrustLevel::Verified
+        );
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3418:
         let event_left = graph_left.observe_repository(left).unwrap();
         let event_right = graph_right.observe_repository(right).unwrap();
         assert_eq!(event_left, event_right);
-        assert_eq!(graph_left.graph_commitment(), graph_right.graph_commitment());
+        assert_eq!(
+            graph_left.graph_commitment(),
+            graph_right.graph_commitment()
+        );
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3470:
                 },
             }],
         };
-        assert!(matches!(graph.apply_event(event), Err(WorkGraphError::InvalidInput(_))));
+        assert!(matches!(
+            graph.apply_event(event),
+            Err(WorkGraphError::InvalidInput(_))
+        ));
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3488:
         });
         graph.observe_repository(obs).unwrap();
         let workstream_id = graph.unfinished_work()[0].node_id.clone();
-        let receipt = graph.handoff_receipt(&workstream_id, "claude", "codex", 2_000).unwrap();
-        assert!(graph.verify_handoff_receipt_against_graph(&receipt).unwrap());
+        let receipt = graph
+            .handoff_receipt(&workstream_id, "claude", "codex", 2_000)
+            .unwrap();
+        assert!(graph
+            .verify_handoff_receipt_against_graph(&receipt)
+            .unwrap());
 
         let mut newer = clean_observation();
         newer.observed_at_ms = 3_000;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3496:
         graph.observe_repository(newer).unwrap();
         assert!(WorkGraph::verify_handoff_receipt(&receipt).unwrap());
-        assert!(!graph.verify_handoff_receipt_against_graph(&receipt).unwrap());
+        assert!(!graph
+            .verify_handoff_receipt_against_graph(&receipt)
+            .unwrap());
 
         let foreign = WorkGraph::new("repo-2").unwrap();
-        assert!(!foreign.verify_handoff_receipt_against_graph(&receipt).unwrap());
+        assert!(!foreign
+            .verify_handoff_receipt_against_graph(&receipt)
+            .unwrap());
     }
-
 }
 
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:54:
             Self::InvalidInput(msg) => write!(f, "invalid work-graph input: {msg}"),
             Self::LimitExceeded(msg) => write!(f, "work-graph limit exceeded: {msg}"),
             Self::RepoMismatch { expected, actual } => {
-                write!(f, "work-graph repo mismatch: expected {expected:?}, got {actual:?}")
+                write!(
+                    f,
+                    "work-graph repo mismatch: expected {expected:?}, got {actual:?}"
+                )
             }
             Self::IntegrityMismatch { expected, actual } => write!(
                 f,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:204:
     }
 }
 
-#[derive(Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq, PartialOrd, Ord, Hash, Default)]
+#[derive(
+    Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq, PartialOrd, Ord, Hash, Default,
+)]
 #[serde(rename_all = "snake_case")]
 pub enum WorkStatus {
     #[default]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:244:
     Other,
 }
 
-
 #[derive(Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq, Hash)]
 #[serde(rename_all = "snake_case")]
 pub enum VerificationState {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:350:
 #[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
 #[serde(tag = "op", rename_all = "snake_case")]
 pub enum WorkOperation {
-    AddEvidence { evidence: EvidenceRef },
-    UpsertNode { node: WorkNode },
-    UpsertEdge { edge: WorkEdge },
+    AddEvidence {
+        evidence: EvidenceRef,
+    },
+    UpsertNode {
+        node: WorkNode,
+    },
+    UpsertEdge {
+        edge: WorkEdge,
+    },
     SetStatus {
         node_id: String,
         status: WorkStatus,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:748:
         self.events.push(event.clone());
 
         let result = if append_in_order {
-            self.apply_materialized(&event).and_then(|_| self.refresh_commitment())
+            self.apply_materialized(&event)
+                .and_then(|_| self.refresh_commitment())
         } else {
             self.rebuild()
         };
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:948:
         }
     }
 
-    pub fn resume(&self, workstream_id: Option<&str>, max_evidence: usize) -> Result<ResumeView, WorkGraphError> {
+    pub fn resume(
+        &self,
+        workstream_id: Option<&str>,
+        max_evidence: usize,
+    ) -> Result<ResumeView, WorkGraphError> {
         let selected = if let Some(id) = workstream_id.filter(|id| !id.is_empty()) {
             let node = self
                 .nodes
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:961:
             }
             self.work_item_view(node)
         } else {
-            self.unfinished_work()
-                .into_iter()
-                .next()
-                .ok_or_else(|| WorkGraphError::UnknownNode("no unfinished workstream".to_string()))?
+            self.unfinished_work().into_iter().next().ok_or_else(|| {
+                WorkGraphError::UnknownNode("no unfinished workstream".to_string())
+            })?
         };
 
         let related = self.connected_node_ids(&selected.node_id, 2, 10_000);
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1045:
             symbols: Vec<String>,
         }
         let mut leases = Vec::new();
-        for node in self.nodes.values().filter(|n| n.kind == NodeKind::WorkLease) {
+        for node in self
+            .nodes
+            .values()
+            .filter(|n| n.kind == NodeKind::WorkLease)
+        {
             let expires = attr_i64(&node.attributes, "expires_at_ms").unwrap_or(0);
             if expires <= now_ms {
                 continue;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1218:
             return Ok(false);
         };
         if workstream.kind != NodeKind::Workstream
-            || receipt.node_ids.binary_search(&receipt.workstream_id).is_err()
+            || receipt
+                .node_ids
+                .binary_search(&receipt.workstream_id)
+                .is_err()
         {
             return Ok(false);
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1225:
         if receipt.node_ids.windows(2).any(|pair| pair[0] >= pair[1])
             || receipt.edge_ids.windows(2).any(|pair| pair[0] >= pair[1])
-            || receipt.evidence_ids.windows(2).any(|pair| pair[0] >= pair[1])
+            || receipt
+                .evidence_ids
+                .windows(2)
+                .any(|pair| pair[0] >= pair[1])
         {
             return Ok(false);
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1231:
-        if receipt.node_ids.iter().any(|id| !self.nodes.contains_key(id))
-            || receipt.edge_ids.iter().any(|id| !self.edges.contains_key(id))
+        if receipt
+            .node_ids
+            .iter()
+            .any(|id| !self.nodes.contains_key(id))
             || receipt
+                .edge_ids
+                .iter()
+                .any(|id| !self.edges.contains_key(id))
+            || receipt
                 .evidence_ids
                 .iter()
                 .any(|id| !self.evidence.contains_key(id))
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1274:
     }
 
     fn rebuild(&mut self) -> Result<(), WorkGraphError> {
-        self.events
-            .sort_by(|a, b| a.observed_at_ms.cmp(&b.observed_at_ms).then_with(|| a.event_id.cmp(&b.event_id)));
+        self.events.sort_by(|a, b| {
+            a.observed_at_ms
+                .cmp(&b.observed_at_ms)
+                .then_with(|| a.event_id.cmp(&b.event_id))
+        });
         self.nodes.clear();
         self.edges.clear();
         self.evidence.clear();
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1316:
                     Some(existing) => {
                         let may_overwrite = incoming.trust >= existing.trust;
                         existing.trust = existing.trust.max(incoming.trust);
-                        existing.observed_at_ms = existing.observed_at_ms.max(incoming.observed_at_ms);
+                        existing.observed_at_ms =
+                            existing.observed_at_ms.max(incoming.observed_at_ms);
                         if may_overwrite {
                             for (key, value) in incoming.attributes {
                                 existing.attributes.insert(key, value);
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1384:
                         self.edges.insert(incoming.edge_id.clone(), incoming);
                     }
                 }
-                self.adjacency.entry(from.clone()).or_default().insert(to.clone());
+                self.adjacency
+                    .entry(from.clone())
+                    .or_default()
+                    .insert(to.clone());
                 self.adjacency.entry(to).or_default().insert(from);
             }
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1409:
                         node.status = *status;
                         node.status_trust = *trust;
                         if !reason.is_empty() {
-                            node.attributes.insert(
-                                "status_reason".to_string(),
-                                Value::String(reason.clone()),
-                            );
+                            node.attributes
+                                .insert("status_reason".to_string(), Value::String(reason.clone()));
                         }
                     }
                 }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1645:
         let mut verification_ids = BTreeSet::new();
         let mut evidence_ids = node.evidence_ids.clone();
         for id in &related {
-            let Some(n) = self.nodes.get(id) else { continue };
+            let Some(n) = self.nodes.get(id) else {
+                continue;
+            };
             evidence_ids.extend(n.evidence_ids.iter().cloned());
             match n.kind {
                 NodeKind::Task => {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1693:
     }
 }
 
-fn observation_to_event(repo_id: &str, mut obs: RepositoryObservation) -> Result<WorkEvent, WorkGraphError> {
+fn observation_to_event(
+    repo_id: &str,
+    mut obs: RepositoryObservation,
+) -> Result<WorkEvent, WorkGraphError> {
     validate_text("repo_id", &obs.repo_id, MAX_ID_LEN, false)?;
-    validate_text("repository_label", &obs.repository_label, MAX_LABEL_LEN, true)?;
+    validate_text(
+        "repository_label",
+        &obs.repository_label,
+        MAX_LABEL_LEN,
+        true,
+    )?;
     validate_text("agent_id", &obs.agent_id, MAX_ID_LEN, true)?;
     validate_text("session_id", &obs.session_id, MAX_ID_LEN, true)?;
     if obs.changes.len() > MAX_OPERATIONS_PER_EVENT / 2 {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1702:
-        return Err(WorkGraphError::LimitExceeded("too many file changes in one observation".to_string()));
+        return Err(WorkGraphError::LimitExceeded(
+            "too many file changes in one observation".to_string(),
+        ));
     }
     if obs.leases.len() > MAX_SCOPE_ITEMS {
-        return Err(WorkGraphError::LimitExceeded("too many work leases".to_string()));
+        return Err(WorkGraphError::LimitExceeded(
+            "too many work leases".to_string(),
+        ));
     }
 
     // Adapters discover filesystem/Git/provider facts in different orders.
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1804:
         repo_attrs.insert("branch".to_string(), Value::String(obs.branch.name.clone()));
     }
     if !obs.branch.head_sha.is_empty() {
-        repo_attrs.insert("head_sha".to_string(), Value::String(obs.branch.head_sha.clone()));
+        repo_attrs.insert(
+            "head_sha".to_string(),
+            Value::String(obs.branch.head_sha.clone()),
+        );
     }
     operations.push(WorkOperation::UpsertNode {
         node: WorkNode {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1850:
         });
     }
 
-    let (task_id, task_label, task_trust, task_explicit_status, remaining_work, task_source_kind, task_source) =
-        if let Some(task) = obs.task_hint.take() {
-            validate_text("task title", &task.title, MAX_LABEL_LEN, false)?;
-            let task_id = if task.task_id.is_empty() {
-                stable_node_id(NodeKind::Task, repo_id, &task.title)
-            } else {
-                validate_text("task_id", &task.task_id, MAX_ID_LEN, false)?;
-                stable_node_id(NodeKind::Task, repo_id, &task.task_id)
-            };
-            (
-                task_id,
-                task.title,
-                task.trust,
-                task.explicit_status,
-                task.remaining_work,
-                task.source_kind,
-                task.source_ref,
-            )
+    let (
+        task_id,
+        task_label,
+        task_trust,
+        task_explicit_status,
+        remaining_work,
+        task_source_kind,
+        task_source,
+    ) = if let Some(task) = obs.task_hint.take() {
+        validate_text("task title", &task.title, MAX_LABEL_LEN, false)?;
+        let task_id = if task.task_id.is_empty() {
+            stable_node_id(NodeKind::Task, repo_id, &task.title)
         } else {
-            let branch_label = if !obs.branch.name.is_empty() {
-                obs.branch.name.clone()
-            } else {
-                "detached-head".to_string()
-            };
-            let label = if meaningful_git {
-                format!("Repository work on {branch_label}")
-            } else {
-                "Repository work".to_string()
-            };
-            (
-                stable_node_id(NodeKind::Task, repo_id, &format!("inferred:{branch_label}")),
-                label,
-                TrustLevel::Inferred,
-                WorkStatus::Unknown,
-                Vec::new(),
-                EvidenceKind::RepositoryFact,
-                "inferred:repository-state".to_string(),
-            )
+            validate_text("task_id", &task.task_id, MAX_ID_LEN, false)?;
+            stable_node_id(NodeKind::Task, repo_id, &task.task_id)
         };
+        (
+            task_id,
+            task.title,
+            task.trust,
+            task.explicit_status,
+            task.remaining_work,
+            task.source_kind,
+            task.source_ref,
+        )
+    } else {
+        let branch_label = if !obs.branch.name.is_empty() {
+            obs.branch.name.clone()
+        } else {
+            "detached-head".to_string()
+        };
+        let label = if meaningful_git {
+            format!("Repository work on {branch_label}")
+        } else {
+            "Repository work".to_string()
+        };
+        (
+            stable_node_id(NodeKind::Task, repo_id, &format!("inferred:{branch_label}")),
+            label,
+            TrustLevel::Inferred,
+            WorkStatus::Unknown,
+            Vec::new(),
+            EvidenceKind::RepositoryFact,
+            "inferred:repository-state".to_string(),
+        )
+    };
 
     let workstream_key = format!("{}:{}", obs.branch.name, task_id);
     let workstream_id = stable_node_id(NodeKind::Workstream, repo_id, &workstream_key);
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1932:
 
     let mut workstream_attrs = BTreeMap::new();
     workstream_attrs.insert("branch".to_string(), Value::String(obs.branch.name.clone()));
-    workstream_attrs.insert("head_sha".to_string(), Value::String(obs.branch.head_sha.clone()));
-    workstream_attrs.insert("base_ref".to_string(), Value::String(obs.branch.base_ref.clone()));
+    workstream_attrs.insert(
+        "head_sha".to_string(),
+        Value::String(obs.branch.head_sha.clone()),
+    );
+    workstream_attrs.insert(
+        "base_ref".to_string(),
+        Value::String(obs.branch.base_ref.clone()),
+    );
     workstream_attrs.insert("ahead_by".to_string(), Value::from(obs.branch.ahead_by));
     workstream_attrs.insert("behind_by".to_string(), Value::from(obs.branch.behind_by));
     workstream_attrs.insert(
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1949:
             status: WorkStatus::Unknown,
             status_trust: TrustLevel::Untrusted,
             attributes: workstream_attrs,
-            evidence_ids: BTreeSet::from([repo_evidence.evidence_id.clone(), task_evidence.evidence_id.clone()]),
+            evidence_ids: BTreeSet::from([
+                repo_evidence.evidence_id.clone(),
+                task_evidence.evidence_id.clone(),
+            ]),
             updated_at_ms: obs.observed_at_ms,
         },
     });
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:1999:
     if !obs.session_id.is_empty() {
         let session_id = stable_node_id(NodeKind::Session, repo_id, &obs.session_id);
         let mut attrs = BTreeMap::new();
-        attrs.insert("session_id".to_string(), Value::String(obs.session_id.clone()));
+        attrs.insert(
+            "session_id".to_string(),
+            Value::String(obs.session_id.clone()),
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: session_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2034:
     }
 
     let mut blocked = obs.branch.merge_in_progress || obs.branch.rebase_in_progress;
-    let mut blocked_trust = if blocked { TrustLevel::Observed } else { TrustLevel::Untrusted };
+    let mut blocked_trust = if blocked {
+        TrustLevel::Observed
+    } else {
+        TrustLevel::Untrusted
+    };
     let mut has_passing_verification = false;
     let mut status_evidence_ids = BTreeSet::from([
         repo_evidence.evidence_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2049:
         let source_ref = format!("git-status:{}", obs.branch.head_sha);
         let mut ev_attrs = BTreeMap::new();
         ev_attrs.insert("path".to_string(), Value::String(change.path.clone()));
-        ev_attrs.insert("change_kind".to_string(), serde_json::to_value(change.kind)?);
+        ev_attrs.insert(
+            "change_kind".to_string(),
+            serde_json::to_value(change.kind)?,
+        );
         let evidence = with_evidence_id(EvidenceRef {
             evidence_id: String::new(),
             kind: EvidenceKind::GitStatus,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2066:
         let file_id = stable_node_id(NodeKind::File, repo_id, &change.path);
         let mut file_attrs = BTreeMap::new();
         file_attrs.insert("path".to_string(), Value::String(change.path.clone()));
-        file_attrs.insert("change_kind".to_string(), serde_json::to_value(change.kind)?);
+        file_attrs.insert(
+            "change_kind".to_string(),
+            serde_json::to_value(change.kind)?,
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: file_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2087:
         change_attrs.insert("staged".to_string(), Value::Bool(change.staged));
         change_attrs.insert("conflicted".to_string(), Value::Bool(change.conflicted));
         if !change.old_path.is_empty() {
-            change_attrs.insert("old_path".to_string(), Value::String(change.old_path.clone()));
+            change_attrs.insert(
+                "old_path".to_string(),
+                Value::String(change.old_path.clone()),
+            );
         }
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2122:
             blocked = true;
             blocked_trust = blocked_trust.max(TrustLevel::Observed);
             status_evidence_ids.insert(evidence.evidence_id.clone());
-            let failure_id = stable_node_id(NodeKind::Failure, repo_id, &format!("conflict:{}", change.path));
+            let failure_id = stable_node_id(
+                NodeKind::Failure,
+                repo_id,
+                &format!("conflict:{}", change.path),
+            );
             operations.push(WorkOperation::UpsertNode {
                 node: WorkNode {
                     node_id: failure_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2156:
             digest: commit.sha.clone(),
             locator: String::new(),
             trust: TrustLevel::Observed,
-            observed_at_ms: if commit.timestamp_ms == 0 { obs.observed_at_ms } else { commit.timestamp_ms },
+            observed_at_ms: if commit.timestamp_ms == 0 {
+                obs.observed_at_ms
+            } else {
+                commit.timestamp_ms
+            },
             attributes: BTreeMap::new(),
         })?;
-        operations.push(WorkOperation::AddEvidence { evidence: evidence.clone() });
+        operations.push(WorkOperation::AddEvidence {
+            evidence: evidence.clone(),
+        });
         let commit_id = stable_node_id(NodeKind::Commit, repo_id, &commit.sha);
         let mut attrs = BTreeMap::new();
         attrs.insert("sha".to_string(), Value::String(commit.sha.clone()));
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2166:
-        attrs.insert("subject".to_string(), Value::String(bounded(&commit.subject, MAX_LABEL_LEN)));
-        attrs.insert("changed_paths".to_string(), serde_json::to_value(&commit.changed_paths)?);
+        attrs.insert(
+            "subject".to_string(),
+            Value::String(bounded(&commit.subject, MAX_LABEL_LEN)),
+        );
+        attrs.insert(
+            "changed_paths".to_string(),
+            serde_json::to_value(&commit.changed_paths)?,
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: commit_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2171:
                 kind: NodeKind::Commit,
-                label: if commit.subject.is_empty() { commit.sha.clone() } else { bounded(&commit.subject, MAX_LABEL_LEN) },
+                label: if commit.subject.is_empty() {
+                    commit.sha.clone()
+                } else {
+                    bounded(&commit.subject, MAX_LABEL_LEN)
+                },
                 trust: TrustLevel::Observed,
                 status: WorkStatus::Unknown,
                 status_trust: TrustLevel::Untrusted,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2178:
                 updated_at_ms: obs.observed_at_ms,
             },
         });
-        operations.push(edge_op(&commit_id, &workstream_id, EdgeKind::PartOf, TrustLevel::Observed, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+        operations.push(edge_op(
+            &commit_id,
+            &workstream_id,
+            EdgeKind::PartOf,
+            TrustLevel::Observed,
+            obs.observed_at_ms,
+            std::slice::from_ref(&evidence.evidence_id),
+        ));
         if !agent_node_id.is_empty() {
-            operations.push(edge_op(&commit_id, &agent_node_id, EdgeKind::ProducedBy, TrustLevel::Observed, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+            operations.push(edge_op(
+                &commit_id,
+                &agent_node_id,
+                EdgeKind::ProducedBy,
+                TrustLevel::Observed,
+                obs.observed_at_ms,
+                std::slice::from_ref(&evidence.evidence_id),
+            ));
         }
         for path in &commit.changed_paths {
             if validate_path(path).is_err() {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2202:
                     updated_at_ms: obs.observed_at_ms,
                 },
             });
-            operations.push(edge_op(&commit_id, &file_id, EdgeKind::Changed, TrustLevel::Observed, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+            operations.push(edge_op(
+                &commit_id,
+                &file_id,
+                EdgeKind::Changed,
+                TrustLevel::Observed,
+                obs.observed_at_ms,
+                std::slice::from_ref(&evidence.evidence_id),
+            ));
         }
     }
 
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2209:
     for verification in &obs.verifications {
         let ev_kind = match verification.evidence_kind {
-            EvidenceKind::TestResult | EvidenceKind::CiResult | EvidenceKind::RavsOutcome => verification.evidence_kind,
+            EvidenceKind::TestResult | EvidenceKind::CiResult | EvidenceKind::RavsOutcome => {
+                verification.evidence_kind
+            }
             _ => EvidenceKind::TestResult,
         };
         let evidence = with_evidence_id(EvidenceRef {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2218:
             digest: verification.digest.clone(),
             locator: verification.name.clone(),
             trust: TrustLevel::Verified,
-            observed_at_ms: if verification.observed_at_ms == 0 { obs.observed_at_ms } else { verification.observed_at_ms },
+            observed_at_ms: if verification.observed_at_ms == 0 {
+                obs.observed_at_ms
+            } else {
+                verification.observed_at_ms
+            },
             attributes: BTreeMap::new(),
         })?;
-        operations.push(WorkOperation::AddEvidence { evidence: evidence.clone() });
-        let kind = if ev_kind == EvidenceKind::CiResult { NodeKind::CiRun } else { NodeKind::Test };
+        operations.push(WorkOperation::AddEvidence {
+            evidence: evidence.clone(),
+        });
+        let kind = if ev_kind == EvidenceKind::CiResult {
+            NodeKind::CiRun
+        } else {
+            NodeKind::Test
+        };
         let verification_key = if verification.verification_id.is_empty() {
             format!("{}:{}", verification.name, verification.source_ref)
         } else {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2230:
         };
         let verification_id = stable_node_id(kind, repo_id, &verification_key);
         let mut attrs = BTreeMap::new();
-        attrs.insert("verification_state".to_string(), serde_json::to_value(verification.state)?);
+        attrs.insert(
+            "verification_state".to_string(),
+            serde_json::to_value(verification.state)?,
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: verification_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2242:
                     VerificationState::Failed => WorkStatus::Blocked,
                     _ => WorkStatus::Unknown,
                 },
-                status_trust: if matches!(verification.state, VerificationState::Passed | VerificationState::Failed) {
+                status_trust: if matches!(
+                    verification.state,
+                    VerificationState::Passed | VerificationState::Failed
+                ) {
                     TrustLevel::Verified
                 } else {
                     TrustLevel::Untrusted
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2252:
                 updated_at_ms: obs.observed_at_ms,
             },
         });
-        operations.push(edge_op(&workstream_id, &verification_id, EdgeKind::VerifiedBy, TrustLevel::Verified, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+        operations.push(edge_op(
+            &workstream_id,
+            &verification_id,
+            EdgeKind::VerifiedBy,
+            TrustLevel::Verified,
+            obs.observed_at_ms,
+            std::slice::from_ref(&evidence.evidence_id),
+        ));
         match verification.state {
             VerificationState::Passed => {
                 has_passing_verification = true;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2262:
                 status_evidence_ids.insert(evidence.evidence_id.clone());
                 blocked = true;
                 blocked_trust = TrustLevel::Verified;
-                let failure_id = stable_node_id(NodeKind::Failure, repo_id, &format!("verification:{verification_id}"));
+                let failure_id = stable_node_id(
+                    NodeKind::Failure,
+                    repo_id,
+                    &format!("verification:{verification_id}"),
+                );
                 operations.push(WorkOperation::UpsertNode {
                     node: WorkNode {
                         node_id: failure_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2276:
                         updated_at_ms: obs.observed_at_ms,
                     },
                 });
-                operations.push(edge_op(&failure_id, &workstream_id, EdgeKind::Blocks, TrustLevel::Verified, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+                operations.push(edge_op(
+                    &failure_id,
+                    &workstream_id,
+                    EdgeKind::Blocks,
+                    TrustLevel::Verified,
+                    obs.observed_at_ms,
+                    std::slice::from_ref(&evidence.evidence_id),
+                ));
             }
             _ => {}
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2294:
             observed_at_ms: obs.observed_at_ms,
             attributes: BTreeMap::new(),
         })?;
-        operations.push(WorkOperation::AddEvidence { evidence: evidence.clone() });
+        operations.push(WorkOperation::AddEvidence {
+            evidence: evidence.clone(),
+        });
         let decision_key = if decision.decision_id.is_empty() {
             format!("{task_id}:{}", decision.text)
         } else {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2314:
                 updated_at_ms: obs.observed_at_ms,
             },
         });
-        operations.push(edge_op(&decision_id, &workstream_id, EdgeKind::PartOf, decision.trust, obs.observed_at_ms, std::slice::from_ref(&evidence.evidence_id)));
+        operations.push(edge_op(
+            &decision_id,
+            &workstream_id,
+            EdgeKind::PartOf,
+            decision.trust,
+            obs.observed_at_ms,
+            std::slice::from_ref(&evidence.evidence_id),
+        ));
     }
 
     for claim in &obs.claims {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2321:
         validate_text("claim", &claim.text, MAX_LABEL_LEN, false)?;
-        let risk = if claim.risk.is_finite() { claim.risk.clamp(0.0, 1.0) } else { 1.0 };
+        let risk = if claim.risk.is_finite() {
+            claim.risk.clamp(0.0, 1.0)
+        } else {
+            1.0
+        };
         // Claim state never manufactures trust. The adapter must explicitly
         // state how the assessment was established (for example WITNESS/RAVS).
         // Raw agent/user statements therefore remain untrusted by default.
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2331:
         };
         let claim_id = stable_node_id(NodeKind::Claim, repo_id, &claim_key);
         let mut attrs = BTreeMap::new();
-        attrs.insert("claim_state".to_string(), serde_json::to_value(claim.state)?);
+        attrs.insert(
+            "claim_state".to_string(),
+            serde_json::to_value(claim.state)?,
+        );
         attrs.insert("risk".to_string(), Value::from(risk));
-        attrs.insert("source_ref".to_string(), Value::String(claim.source_ref.clone()));
+        attrs.insert(
+            "source_ref".to_string(),
+            Value::String(claim.source_ref.clone()),
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: claim_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2340:
                 kind: NodeKind::Claim,
                 label: claim.text.clone(),
                 trust,
-                status: if claim.state == ClaimState::Contradicted { WorkStatus::Blocked } else { WorkStatus::Unknown },
-                status_trust: if claim.state == ClaimState::Contradicted { trust } else { TrustLevel::Untrusted },
+                status: if claim.state == ClaimState::Contradicted {
+                    WorkStatus::Blocked
+                } else {
+                    WorkStatus::Unknown
+                },
+                status_trust: if claim.state == ClaimState::Contradicted {
+                    trust
+                } else {
+                    TrustLevel::Untrusted
+                },
                 attributes: attrs,
                 evidence_ids: claim.evidence_ids.iter().cloned().collect(),
                 updated_at_ms: obs.observed_at_ms,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2348:
             },
         });
-        operations.push(edge_op(&claim_id, &workstream_id, EdgeKind::PartOf, trust, obs.observed_at_ms, &claim.evidence_ids));
+        operations.push(edge_op(
+            &claim_id,
+            &workstream_id,
+            EdgeKind::PartOf,
+            trust,
+            obs.observed_at_ms,
+            &claim.evidence_ids,
+        ));
         if claim.state == ClaimState::Contradicted {
-            let failure_id = stable_node_id(NodeKind::Failure, repo_id, &format!("claim:{claim_id}"));
+            let failure_id =
+                stable_node_id(NodeKind::Failure, repo_id, &format!("claim:{claim_id}"));
             operations.push(WorkOperation::UpsertNode {
                 node: WorkNode {
                     node_id: failure_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2363:
                     updated_at_ms: obs.observed_at_ms,
                 },
             });
-            operations.push(edge_op(&claim_id, &failure_id, EdgeKind::ContradictedBy, trust, obs.observed_at_ms, &claim.evidence_ids));
+            operations.push(edge_op(
+                &claim_id,
+                &failure_id,
+                EdgeKind::ContradictedBy,
+                trust,
+                obs.observed_at_ms,
+                &claim.evidence_ids,
+            ));
         }
     }
 
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2370:
     for lease in &obs.leases {
         validate_text("lease agent", &lease.agent_id, MAX_ID_LEN, false)?;
         if lease.scope_paths.len() + lease.scope_symbols.len() > MAX_SCOPE_ITEMS {
-            return Err(WorkGraphError::LimitExceeded("lease scope too large".to_string()));
+            return Err(WorkGraphError::LimitExceeded(
+                "lease scope too large".to_string(),
+            ));
         }
         for path in &lease.scope_paths {
             validate_path(path)?;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2407:
             attributes: BTreeMap::new(),
         })?;
         let lease_evidence_id = lease_evidence.evidence_id.clone();
-        operations.push(WorkOperation::AddEvidence { evidence: lease_evidence });
+        operations.push(WorkOperation::AddEvidence {
+            evidence: lease_evidence,
+        });
         let mut attrs = BTreeMap::new();
-        attrs.insert("agent_id".to_string(), Value::String(lease.agent_id.clone()));
         attrs.insert(
+            "agent_id".to_string(),
+            Value::String(lease.agent_id.clone()),
+        );
+        attrs.insert(
             "task_id".to_string(),
-            Value::String(if lease.task_id.is_empty() { task_id.clone() } else { lease.task_id.clone() }),
+            Value::String(if lease.task_id.is_empty() {
+                task_id.clone()
+            } else {
+                lease.task_id.clone()
+            }),
         );
-        attrs.insert("scope_paths".to_string(), serde_json::to_value(&lease.scope_paths)?);
-        attrs.insert("scope_symbols".to_string(), serde_json::to_value(&lease.scope_symbols)?);
-        attrs.insert("expires_at_ms".to_string(), Value::from(lease.expires_at_ms));
+        attrs.insert(
+            "scope_paths".to_string(),
+            serde_json::to_value(&lease.scope_paths)?,
+        );
+        attrs.insert(
+            "scope_symbols".to_string(),
+            serde_json::to_value(&lease.scope_symbols)?,
+        );
+        attrs.insert(
+            "expires_at_ms".to_string(),
+            Value::from(lease.expires_at_ms),
+        );
         attrs.insert("source_ref".to_string(), Value::String(lease_source_ref));
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2482:
             },
         });
         let execution_key = if execution.execution_id.is_empty() {
-            format!("{}:{}:{}:{}", task_id, execution.provider, execution.model, execution.source_ref)
+            format!(
+                "{}:{}:{}:{}",
+                task_id, execution.provider, execution.model, execution.source_ref
+            )
         } else {
             execution.execution_id.clone()
         };
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2510:
             attributes: BTreeMap::new(),
         })?;
         let execution_evidence_id = execution_evidence.evidence_id.clone();
-        operations.push(WorkOperation::AddEvidence { evidence: execution_evidence });
+        operations.push(WorkOperation::AddEvidence {
+            evidence: execution_evidence,
+        });
         let mut attrs = BTreeMap::new();
-        attrs.insert("provider".to_string(), Value::String(execution.provider.clone()));
+        attrs.insert(
+            "provider".to_string(),
+            Value::String(execution.provider.clone()),
+        );
         attrs.insert("model".to_string(), Value::String(execution.model.clone()));
         attrs.insert("latency_ms".to_string(), Value::from(execution.latency_ms));
-        attrs.insert("cost_micro_usd".to_string(), Value::from(execution.cost_micro_usd));
+        attrs.insert(
+            "cost_micro_usd".to_string(),
+            Value::from(execution.cost_micro_usd),
+        );
         if let Some(success) = execution.success {
             attrs.insert("success".to_string(), Value::Bool(success));
         }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2522:
-        attrs.insert("source_ref".to_string(), Value::String(execution_source_ref));
+        attrs.insert(
+            "source_ref".to_string(),
+            Value::String(execution_source_ref),
+        );
         operations.push(WorkOperation::UpsertNode {
             node: WorkNode {
                 node_id: execution_id.clone(),
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2531:
                     Some(false) => WorkStatus::Blocked,
                     None => WorkStatus::Unknown,
                 },
-                status_trust: if execution.success.is_some() { TrustLevel::Observed } else { TrustLevel::Untrusted },
+                status_trust: if execution.success.is_some() {
+                    TrustLevel::Observed
+                } else {
+                    TrustLevel::Untrusted
+                },
                 attributes: attrs,
                 evidence_ids: BTreeSet::from([execution_evidence_id.clone()]),
                 updated_at_ms: obs.observed_at_ms,
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2575:
         }
     } else if task_explicit_status != WorkStatus::Unknown {
         task_explicit_status
-    } else if meaningful_git || !obs.decisions.is_empty() || !obs.leases.is_empty() || !obs.model_executions.is_empty() {
+    } else if meaningful_git
+        || !obs.decisions.is_empty()
+        || !obs.leases.is_empty()
+        || !obs.model_executions.is_empty()
+    {
         WorkStatus::InProgress
     } else if had_explicit_task {
         WorkStatus::Planned
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2585:
     let status_reason = match status {
         WorkStatus::Blocked => "durable conflict or failing verification observed",
         WorkStatus::Completed => "explicit completion with passing verification",
-        WorkStatus::NeedsVerification => "completion was stated but independent verification is missing",
+        WorkStatus::NeedsVerification => {
+            "completion was stated but independent verification is missing"
+        }
         WorkStatus::Planned => "explicit task observed before durable work changes",
         WorkStatus::InProgress => "durable work evidence observed",
         WorkStatus::Abandoned => "task explicitly marked abandoned",
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2736:
 
 fn validate_event(event: &mut WorkEvent) -> Result<(), WorkGraphError> {
     validate_text("event_id", &event.event_id, 128, true)?;
-    validate_text("event source_ref", &event.source_ref, MAX_SOURCE_REF_LEN, true)?;
+    validate_text(
+        "event source_ref",
+        &event.source_ref,
+        MAX_SOURCE_REF_LEN,
+        true,
+    )?;
     validate_text("event actor_id", &event.actor_id, MAX_ID_LEN, true)?;
     validate_text("event session_id", &event.session_id, MAX_ID_LEN, true)?;
     if event.operations.len() > MAX_OPERATIONS_PER_EVENT {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2791:
 
 fn validate_evidence(evidence: &mut EvidenceRef) -> Result<(), WorkGraphError> {
     validate_text("evidence_id", &evidence.evidence_id, MAX_ID_LEN, true)?;
-    validate_text("evidence source_ref", &evidence.source_ref, MAX_SOURCE_REF_LEN, true)?;
+    validate_text(
+        "evidence source_ref",
+        &evidence.source_ref,
+        MAX_SOURCE_REF_LEN,
+        true,
+    )?;
     validate_text("evidence digest", &evidence.digest, MAX_ID_LEN, true)?;
-    validate_text("evidence locator", &evidence.locator, MAX_SOURCE_REF_LEN, true)?;
+    validate_text(
+        "evidence locator",
+        &evidence.locator,
+        MAX_SOURCE_REF_LEN,
+        true,
+    )?;
     if evidence.trust > max_trust_for_evidence_kind(evidence.kind) {
         return Err(WorkGraphError::InvalidInput(format!(
             "evidence kind {:?} cannot directly assert trust {:?}",
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2850:
 
 fn validate_id_set(name: &str, values: &BTreeSet<String>) -> Result<(), WorkGraphError> {
     if values.len() > MAX_SCOPE_ITEMS {
-        return Err(WorkGraphError::LimitExceeded(format!("{name} has too many IDs")));
+        return Err(WorkGraphError::LimitExceeded(format!(
+            "{name} has too many IDs"
+        )));
     }
     for value in values {
         validate_text(name, value, MAX_ID_LEN, false)?;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2892:
     Ok(())
 }
 
-fn validate_text(name: &str, text: &str, max_len: usize, allow_empty: bool) -> Result<(), WorkGraphError> {
+fn validate_text(
+    name: &str,
+    text: &str,
+    max_len: usize,
+    allow_empty: bool,
+) -> Result<(), WorkGraphError> {
     if !allow_empty && text.is_empty() {
-        return Err(WorkGraphError::InvalidInput(format!("{name} must not be empty")));
+        return Err(WorkGraphError::InvalidInput(format!(
+            "{name} must not be empty"
+        )));
     }
     if text.len() > max_len {
         return Err(WorkGraphError::LimitExceeded(format!(
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2975:
             if paths_overlap(left, right) {
                 let left_n = normalize_scope_path(left);
                 let right_n = normalize_scope_path(right);
-                result.insert(if left_n.len() >= right_n.len() { left_n } else { right_n });
+                result.insert(if left_n.len() >= right_n.len() {
+                    left_n
+                } else {
+                    right_n
+                });
             }
         }
     }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:2985:
 fn overlap_exact(a: &[String], b: &[String]) -> Vec<String> {
     let left: BTreeSet<&str> = a.iter().map(String::as_str).collect();
     let right: BTreeSet<&str> = b.iter().map(String::as_str).collect();
-    left.intersection(&right).map(|s| (*s).to_string()).collect()
+    left.intersection(&right)
+        .map(|s| (*s).to_string())
+        .collect()
 }
 
 #[cfg(test)]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3021:
         let mut graph = WorkGraph::new("repo-1").unwrap();
         graph.observe_repository(clean_observation()).unwrap();
         assert!(graph.unfinished_work().is_empty());
-        assert_eq!(graph.nodes.len(), 1, "clean observation should create only repository node");
+        assert_eq!(
+            graph.nodes.len(),
+            1,
+            "clean observation should create only repository node"
+        );
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3041:
         let unfinished = graph.unfinished_work();
         assert_eq!(unfinished.len(), 1);
         assert_eq!(unfinished[0].status, WorkStatus::Planned);
-        assert!(graph.evidence.values().any(|e| e.kind == EvidenceKind::Checkpoint));
+        assert!(graph
+            .evidence
+            .values()
+            .any(|e| e.kind == EvidenceKind::Checkpoint));
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3062:
         let unfinished = graph.unfinished_work();
         assert_eq!(unfinished.len(), 1);
         assert_eq!(unfinished[0].status, WorkStatus::InProgress);
-        assert!(unfinished[0].changed_paths.contains(&"src/auth.rs".to_string()));
+        assert!(unfinished[0]
+            .changed_paths
+            .contains(&"src/auth.rs".to_string()));
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3079:
             source_ref: "checkpoint:1".to_string(),
         });
         graph.observe_repository(obs.clone()).unwrap();
-        assert_eq!(graph.unfinished_work()[0].status, WorkStatus::NeedsVerification);
+        assert_eq!(
+            graph.unfinished_work()[0].status,
+            WorkStatus::NeedsVerification
+        );
 
         obs.observed_at_ms += 1;
         obs.verifications.push(VerificationObservation {
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3093:
         });
         graph.observe_repository(obs).unwrap();
         assert!(graph.unfinished_work().is_empty());
-        assert!(graph.nodes.values().any(|n| n.kind == NodeKind::Task && n.status == WorkStatus::Completed));
+        assert!(graph
+            .nodes
+            .values()
+            .any(|n| n.kind == NodeKind::Task && n.status == WorkStatus::Completed));
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3147:
             evidence_ids: vec![],
         });
         graph.observe_repository(obs).unwrap();
-        let claim = graph.nodes.values().find(|n| n.kind == NodeKind::Claim).unwrap();
+        let claim = graph
+            .nodes
+            .values()
+            .find(|n| n.kind == NodeKind::Claim)
+            .unwrap();
         assert_eq!(claim.trust, TrustLevel::Untrusted);
         assert_eq!(claim.status, WorkStatus::Blocked);
     }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3189:
         let report = graph.coordination_report(2_000);
         assert_eq!(report.active_leases, 2);
         assert_eq!(report.conflicts.len(), 1);
-        assert_eq!(report.conflicts[0].overlapping_symbols, vec!["Auth.refresh"]);
+        assert_eq!(
+            report.conflicts[0].overlapping_symbols,
+            vec!["Auth.refresh"]
+        );
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3213:
         a.observe_repository(one.clone()).unwrap();
         b.observe_repository(two.clone()).unwrap();
         a.merge(&b).unwrap();
-        b.merge(&WorkGraph::from_json(&a.export_json(false).unwrap()).unwrap()).unwrap();
+        b.merge(&WorkGraph::from_json(&a.export_json(false).unwrap()).unwrap())
+            .unwrap();
         assert_eq!(a.graph_commitment(), b.graph_commitment());
         assert_eq!(a.event_count(), 2);
     }
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3257:
         });
         graph.observe_repository(obs).unwrap();
         let id = graph.unfinished_work()[0].node_id.clone();
-        let mut receipt = graph.handoff_receipt(&id, "claude", "codex", 2_000).unwrap();
+        let mut receipt = graph
+            .handoff_receipt(&id, "claude", "codex", 2_000)
+            .unwrap();
         assert!(WorkGraph::verify_handoff_receipt(&receipt).unwrap());
         receipt.to_agent = "other".to_string();
         assert!(!WorkGraph::verify_handoff_receipt(&receipt).unwrap());
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3322:
             .node_id
             .clone();
         assert_eq!(graph.nodes[&workstream_id].status, WorkStatus::Completed);
-        assert_eq!(graph.nodes[&workstream_id].status_trust, TrustLevel::Verified);
+        assert_eq!(
+            graph.nodes[&workstream_id].status_trust,
+            TrustLevel::Verified
+        );
 
         let mut stale = clean_observation();
         stale.observed_at_ms += 1;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3337:
         });
         graph.observe_repository(stale).unwrap();
         assert_eq!(graph.nodes[&workstream_id].status, WorkStatus::Completed);
-        assert_eq!(graph.nodes[&workstream_id].status_trust, TrustLevel::Verified);
+        assert_eq!(
+            graph.nodes[&workstream_id].status_trust,
+            TrustLevel::Verified
+        );
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3418:
         let event_left = graph_left.observe_repository(left).unwrap();
         let event_right = graph_right.observe_repository(right).unwrap();
         assert_eq!(event_left, event_right);
-        assert_eq!(graph_left.graph_commitment(), graph_right.graph_commitment());
+        assert_eq!(
+            graph_left.graph_commitment(),
+            graph_right.graph_commitment()
+        );
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3470:
                 },
             }],
         };
-        assert!(matches!(graph.apply_event(event), Err(WorkGraphError::InvalidInput(_))));
+        assert!(matches!(
+            graph.apply_event(event),
+            Err(WorkGraphError::InvalidInput(_))
+        ));
     }
 
     #[test]
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3488:
         });
         graph.observe_repository(obs).unwrap();
         let workstream_id = graph.unfinished_work()[0].node_id.clone();
-        let receipt = graph.handoff_receipt(&workstream_id, "claude", "codex", 2_000).unwrap();
-        assert!(graph.verify_handoff_receipt_against_graph(&receipt).unwrap());
+        let receipt = graph
+            .handoff_receipt(&workstream_id, "claude", "codex", 2_000)
+            .unwrap();
+        assert!(graph
+            .verify_handoff_receipt_against_graph(&receipt)
+            .unwrap());
 
         let mut newer = clean_observation();
         newer.observed_at_ms = 3_000;
Diff in /home/runner/work/entroly/entroly/entroly-engine/src/work_graph.rs:3496:
         graph.observe_repository(newer).unwrap();
         assert!(WorkGraph::verify_handoff_receipt(&receipt).unwrap());
-        assert!(!graph.verify_handoff_receipt_against_graph(&receipt).unwrap());
+        assert!(!graph
+            .verify_handoff_receipt_against_graph(&receipt)
+            .unwrap());
 
         let foreign = WorkGraph::new("repo-2").unwrap();
-        assert!(!foreign.verify_handoff_receipt_against_graph(&receipt).unwrap());
+        assert!(!foreign
+            .verify_handoff_receipt_against_graph(&receipt)
+            .unwrap());
     }
-
 }
 
== cargo test ==
[1m[92m    Updating[0m crates.io index
[1m[92m     Locking[0m 7 packages to latest compatible versions
[1m[92m      Adding[0m block-buffer v0.10.4
[1m[92m      Adding[0m cpufeatures v0.2.17
[1m[92m      Adding[0m crypto-common v0.1.7
[1m[92m      Adding[0m digest v0.10.7
[1m[92m      Adding[0m generic-array v0.14.7 [1m[33m(available: v0.14.9)[0m
[1m[92m      Adding[0m sha2 v0.10.9 [1m[33m(available: v0.11.0)[0m
[1m[92m      Adding[0m version_check v0.9.5
[1m[92m Downloading[0m crates ...
[1m[92m  Downloaded[0m block-buffer v0.12.1
[1m[92m  Downloaded[0m adler2 v2.0.1
[1m[92m  Downloaded[0m block-buffer v0.10.4
[1m[92m  Downloaded[0m cfg-if v1.0.4
[1m[92m  Downloaded[0m cpufeatures v0.2.17
[1m[92m  Downloaded[0m generic-array v0.14.7
[1m[92m  Downloaded[0m crc32fast v1.5.0
[1m[92m  Downloaded[0m crossbeam-deque v0.8.7
[1m[92m  Downloaded[0m crossbeam-epoch v0.9.20
[1m[92m  Downloaded[0m quote v1.0.47
[1m[92m  Downloaded[0m const-oid v0.10.2
[1m[92m  Downloaded[0m crossbeam-utils v0.8.22
[1m[92m  Downloaded[0m bitflags v2.13.1
[1m[92m  Downloaded[0m miniz_oxide v0.8.9
[1m[92m  Downloaded[0m digest v0.10.7
[1m[92m  Downloaded[0m unicode-ident v1.0.24
[1m[92m  Downloaded[0m crypto-common v0.2.2
[1m[92m  Downloaded[0m md-5 v0.11.0
[1m[92m  Downloaded[0m zmij v1.0.23
[1m[92m  Downloaded[0m crypto-common v0.1.7
[1m[92m  Downloaded[0m itoa v1.0.18
[1m[92m  Downloaded[0m version_check v0.9.5
[1m[92m  Downloaded[0m either v1.17.0
[1m[92m  Downloaded[0m errno v0.3.14
[1m[92m  Downloaded[0m digest v0.11.3
[1m[92m  Downloaded[0m simd-adler32 v0.3.10
[1m[92m  Downloaded[0m fastrand v2.5.0
[1m[92m  Downloaded[0m sha2 v0.10.9
[1m[92m  Downloaded[0m tempfile v3.27.0
[1m[92m  Downloaded[0m once_cell v1.21.4
[1m[92m  Downloaded[0m serde_derive v1.0.229
[1m[92m  Downloaded[0m getrandom v0.4.3
[1m[92m  Downloaded[0m proc-macro2 v1.0.107
[1m[92m  Downloaded[0m rayon-core v1.13.0
[1m[92m  Downloaded[0m serde_core v1.0.229
[1m[92m  Downloaded[0m hybrid-array v0.4.14
[1m[92m  Downloaded[0m serde_json v1.0.151
[1m[92m  Downloaded[0m flate2 v1.1.9
[1m[92m  Downloaded[0m memchr v2.8.3
[1m[92m  Downloaded[0m serde v1.0.229
[1m[92m  Downloaded[0m typenum v1.20.1
[1m[92m  Downloaded[0m rayon v1.12.0
[1m[92m  Downloaded[0m syn v3.0.3
[1m[92m  Downloaded[0m rustix v1.1.4
[1m[92m  Downloaded[0m libc v0.2.189
[1m[92m  Downloaded[0m linux-raw-sys v0.12.1
[1m[92m   Compiling[0m typenum v1.20.1
[1m[92m   Compiling[0m version_check v0.9.5
[1m[92m   Compiling[0m crossbeam-utils v0.8.22
[1m[92m   Compiling[0m proc-macro2 v1.0.107
[1m[92m   Compiling[0m cfg-if v1.0.4
[1m[92m   Compiling[0m generic-array v0.14.7
[1m[92m   Compiling[0m unicode-ident v1.0.24
[1m[92m   Compiling[0m quote v1.0.47
[1m[92m   Compiling[0m crossbeam-epoch v0.9.20
[1m[92m   Compiling[0m hybrid-array v0.4.14
[1m[92m   Compiling[0m serde_core v1.0.229
[1m[92m   Compiling[0m libc v0.2.189
[1m[92m   Compiling[0m crossbeam-deque v0.8.7
[1m[92m   Compiling[0m rustix v1.1.4
[1m[92m   Compiling[0m getrandom v0.4.3
[1m[92m   Compiling[0m crc32fast v1.5.0
[1m[92m   Compiling[0m rayon-core v1.13.0
[1m[92m   Compiling[0m zmij v1.0.23
[1m[92m   Compiling[0m syn v3.0.3
[1m[92m   Compiling[0m crypto-common v0.2.2
[1m[92m   Compiling[0m block-buffer v0.12.1
[1m[92m   Compiling[0m crypto-common v0.1.7
[1m[92m   Compiling[0m block-buffer v0.10.4
[1m[92m   Compiling[0m serde v1.0.229
[1m[92m   Compiling[0m simd-adler32 v0.3.10
[1m[92m   Compiling[0m serde_json v1.0.151
[1m[92m   Compiling[0m const-oid v0.10.2
[1m[92m   Compiling[0m bitflags v2.13.1
[1m[92m   Compiling[0m linux-raw-sys v0.12.1
[1m[92m   Compiling[0m adler2 v2.0.1
[1m[92m   Compiling[0m miniz_oxide v0.8.9
[1m[92m   Compiling[0m serde_derive v1.0.229
[1m[92m   Compiling[0m digest v0.11.3
[1m[92m   Compiling[0m digest v0.10.7
[1m[92m   Compiling[0m either v1.17.0
[1m[92m   Compiling[0m itoa v1.0.18
[1m[92m   Compiling[0m fastrand v2.5.0
[1m[92m   Compiling[0m memchr v2.8.3
[1m[92m   Compiling[0m once_cell v1.21.4
[1m[92m   Compiling[0m cpufeatures v0.2.17
[1m[92m   Compiling[0m sha2 v0.10.9
[1m[92m   Compiling[0m tempfile v3.27.0
[1m[92m   Compiling[0m rayon v1.12.0
[1m[92m   Compiling[0m flate2 v1.1.9
[1m[92m   Compiling[0m md-5 v0.11.0
[1m[92m   Compiling[0m entroly-engine v1.0.78 (/home/runner/work/entroly/entroly/entroly-engine)
[1m[92m    Finished[0m `test` profile [unoptimized + debuginfo] target(s) in 22.05s
[1m[92m     Running[0m unittests src/lib.rs (entroly-engine/target/debug/deps/entroly_engine-1968209668fcf7c8)

running 439 tests
test anomaly::tests::test_small_group_skipped ... ok
test anomaly::tests::test_cross_directory_isolation ... ok
test anomaly::tests::test_drop_detection ... ok
test anomaly::tests::test_uniform_group_no_anomalies ... ok
test bm25::stemming_tests::conservative_rules_do_not_merge_unrelated_words ... ok
test bm25::stemming_tests::known_reductions ... ok
test bm25::stemming_tests::stemming_is_applied_to_both_sides_identically ... ok
test bm25::stemming_tests::natural_language_meets_code_identifiers ... ok
test anomaly::tests::test_spike_detection ... ok
test bm25::tests::test_normalize_scores ... ok
test bm25::tests::test_split_identifier_acronym ... ok
test bm25::tests::test_split_identifier_camel ... ok
test bm25::tests::test_split_identifier_snake ... ok
test bm25::tests::test_tokenize_path ... ok
test cache::tests::test_adaptive_alpha_adapts ... ok
test cache::tests::test_adaptive_alpha_starts_at_2 ... ok
test bm25::tests::test_bm25_relevance_ordering ... ok
test bm25::stemming_tests::stemming_is_deterministic ... ok
test cache::tests::test_bench_adaptivity ... ok
test cache::tests::test_bench_dag_aware_eviction ... ok
test cache::tests::test_bench_distribution_shift_torture ... ok
test cache::tests::test_bench_cost_aware_utility ... ok
test cache::tests::test_bench_ablation_study ... ok
test cache::tests::test_bench_prediction_accuracy ... ok
test cache::tests::test_bench_baseline_gauntlet ... ok
test cache::tests::test_bench_scale_invariance ... ok
test cache::tests::test_cache_persistence_roundtrip ... ok
test cache::tests::test_cascade_tracking ... ok
test cache::tests::test_causal_decay_full_overlap ... ok
test cache::tests::test_causal_decay_monotone_in_overlap ... ok
test cache::tests::test_causal_decay_no_overlap ... ok
test cache::tests::test_causal_decay_partial ... ok
test cache::tests::test_cost_model_expensive_better ... ok
test cache::tests::test_cost_model_high_hit_prob_better ... ok
test cache::tests::test_depth_weighted_decay ... ok
test cache::tests::test_eviction_on_full_cache ... ok
test cache::tests::test_exact_hit ... ok
test cache::tests::test_gc_removes_low_quality ... ok
test cache::tests::test_invalidation_integration ... ok
test cache::tests::test_predictor_learns ... ok
test cache::tests::test_renyi_h2_leq_shannon ... ok
test cache::tests::test_renyi_monotone_in_alpha ... ok
test cache::tests::test_renyi_uniform_equals_log_n ... ok
test cache::tests::test_semantic_hit ... ok
test cache::tests::test_sketch_empty ... ok
test cache::tests::test_sketch_matches_exact ... ok
test cache::tests::test_sketch_single_item ... ok
test cache::tests::test_sketch_uniform_vs_skewed ... ok
test cache::tests::test_stats_comprehensive ... ok
test cache::tests::test_submodular_diminishing_returns ... ok
test cache::tests::test_submodular_evicts_redundant ... ok
test cache::tests::test_submodular_lazy_finds_victim ... ok
test cache::tests::test_thompson_admits_high_entropy ... ok
test cache::tests::test_thompson_posterior_decay ... ok
test cache::tests::test_thompson_posterior_updates ... ok
test cache::tests::test_thompson_rejects_trivial ... ok
test cache::tests::test_warm_start_hit_rate ... ok
test cache::tests::test_wilson_score_converges ... ok
test causal::tests::test_base_rate_tracking ... ok
test causal::tests::test_circular_buffer_capacity ... ok
test causal::tests::test_confidence_increases_with_trials ... ok
test causal::tests::test_confounding_bias_detection ... ok
test causal::tests::test_decay_tick_removes_stale_links ... ok
test causal::tests::test_empty_selected_ignored ... ok
test causal::tests::test_gravity_bonuses_positive_effect ... ok
test causal::tests::test_gravity_zero_for_negative_effect ... ok
test causal::tests::test_interventional_vs_observational_separation ... ok
test causal::tests::test_nan_outcome_ignored ... ok
test causal::tests::test_new_graph_is_empty ... ok
test causal::tests::test_remove_fragments_purges_learned_state ... ok
test causal::tests::test_self_links_excluded ... ok
test causal::tests::test_serde_roundtrip ... ok
test causal::tests::test_single_trace_updates_estimates ... ok
test causal::tests::test_stats_consistency ... ok
test causal::tests::test_temporal_bonuses_computation ... ok
test causal::tests::test_temporal_effect_computation ... ok
test causal::tests::test_temporal_eviction_under_capacity ... ok
test causal::tests::test_temporal_links_created ... ok
test causal::tests::test_top_causal_fragments ... ok
test channel::tests::test_attention_large_context_no_nan_inf ... ok
test channel::tests::test_attention_single_fragment ... ok
test channel::tests::test_attention_symmetry ... ok
test channel::tests::test_attention_u_shaped ... ok
test channel::tests::test_attention_zero_total ... ok
test channel::tests::test_bookend_empty ... ok
test channel::tests::test_bookend_preserves_all_indices ... ok
test channel::tests::test_bookend_small_input ... ok
test channel::tests::test_contradiction_guard_different_sources_no_eviction ... ok
test channel::tests::test_contradiction_guard_empty ... ok
test channel::tests::test_contradiction_guard_no_contradictions ... ok
test channel::tests::test_contradiction_guard_same_source_different_content ... ok
test channel::tests::test_contradiction_guard_single_fragment ... ok
test channel::tests::test_full_pipeline_trailing_then_interleave ... ok
test channel::tests::test_information_reward_empty_inputs ... ok
test channel::tests::test_information_reward_high_util ... ok
test channel::tests::test_information_reward_no_nan_with_zero_entropy ... ok
test channel::tests::test_information_reward_single_fragment ... ok
test channel::tests::test_information_reward_zero_util ... ok
test channel::tests::test_interleave_cyclic_dependencies ... ok
test channel::tests::test_interleave_deep_dependency_chain ... ok
test channel::tests::test_interleave_empty ... ok
test channel::tests::test_interleave_mismatched_relevances ... ok
test channel::tests::test_interleave_preserves_all_indices ... ok
test channel::tests::test_interleave_respects_causal_order ... ok
test channel::tests::test_interleave_single ... ok
test channel::tests::test_interleave_star_dependency ... ok
test channel::tests::test_interleave_two_fragments ... ok
test channel::tests::test_marginal_gain_is_submodular ... ok
test channel::tests::test_modulated_reward_bounds ... ok
test channel::tests::test_modulated_reward_credit_assignment ... ok
test channel::tests::test_modulated_reward_extreme_sufficiency ... ok
test channel::tests::test_modulated_reward_is_always_finite ... ok
test channel::tests::test_trailing_pass_all_already_selected ... ok
test channel::tests::test_trailing_pass_all_candidates_oversized ... ok
test channel::tests::test_trailing_pass_empty_fragment_list ... ok
test channel::tests::test_trailing_pass_fills_gap ... ok
test channel::tests::test_trailing_pass_incremental_submodularity ... ok
test channel::tests::test_trailing_pass_no_duplicate_indices ... ok
test channel::tests::test_trailing_pass_performance_1000_frags ... ok
test channel::tests::test_trailing_pass_respects_budget ... ok
test channel::tests::test_trailing_pass_single_token_gap ... ok
test channel::tests::test_trailing_pass_with_very_short_content ... ok
test channel::tests::test_trailing_pass_zero_entropy_fragments ... ok
test channel::tests::test_trailing_pass_zero_gap_returns_empty ... ok
test conversation_pruner::tests::stress_500_blocks_under_200ms ... ok
test conversation_pruner::tests::stress_budget_boundaries ... ok
test conversation_pruner::tests::stress_compress_all_resolutions_all_kinds ... ok
test conversation_pruner::tests::stress_dag_coherence_post_pruning ... ok
test conversation_pruner::tests::stress_forward_overlap_correctness ... ok
test conversation_pruner::tests::stress_progressive_thresholds_all_utilizations ... ok
test conversation_pruner::tests::stress_protected_messages_invariant ... ok
test conversation_pruner::tests::stress_random_indices_never_panic ... ok
test conversation_pruner::tests::test_200_blocks_under_100ms ... ok
test conversation_pruner::tests::test_budget_exceeds_total ... ok
test conversation_pruner::tests::test_budget_zero ... ok
test conversation_pruner::tests::test_classify_block_types ... ok
test conversation_pruner::tests::test_compress_block_empty_content ... ok
test conversation_pruner::tests::test_compress_digest ... ok
test conversation_pruner::tests::test_compress_fingerprint ... ok
test conversation_pruner::tests::test_compress_skeleton_tool_result ... ok
test conversation_pruner::tests::test_dag_coherence_no_upward_propagation ... ok
test conversation_pruner::tests::test_dag_coherence_propagates ... ok
test conversation_pruner::tests::test_forward_overlap_multiple_blocks ... ok
test conversation_pruner::tests::test_forward_overlap_recent_block_gets_default ... ok
test conversation_pruner::tests::test_infer_assistant_depends_on_tool_result ... ok
test conversation_pruner::tests::test_infer_tool_result_depends_on_tool_call ... ok
test conversation_pruner::tests::test_negative_decay_lambda_clamped ... ok
test conversation_pruner::tests::test_noise_penalty_reduces_value_of_noisy_blocks ... ok
test conversation_pruner::tests::test_non_sequential_block_indices_no_panic ... ok
test conversation_pruner::tests::test_progressive_aggressive_at_92pct ... ok
test conversation_pruner::tests::test_progressive_no_compression_below_70pct ... ok
test conversation_pruner::tests::test_progressive_thresholds_position_based_recency ... ok
test conversation_pruner::tests::test_progressive_tool_results_skeleton_at_75pct ... ok
test conversation_pruner::tests::test_prune_empty ... ok
test conversation_pruner::tests::test_prune_everything_fits ... ok
test conversation_pruner::tests::test_resolution_ordering ... ok
test conversation_pruner::tests::test_system_messages_never_pruned ... ok
test conversation_pruner::tests::test_thinking_blocks_most_aggressively_pruned ... ok
test conversation_pruner::tests::test_tool_results_pruned_first ... ok
test conversation_pruner::tests::test_user_messages_never_pruned ... ok
test dedup::tests::test_candidate_gate_accepts_near_duplicate ... ok
test dedup::tests::test_dedup_allows_different ... ok
test dedup::tests::test_dedup_catches_exact ... ok
test dedup::tests::test_identical_texts ... ok
test dedup::tests::test_lcb_does_not_invent_redundancy_as_set_grows ... ok
test dedup::tests::test_lcb_power_is_limited_by_fragment_length ... ok
test dedup::tests::test_lcb_still_detects_real_duplicates ... ok
test depgraph::tests::test_c_ffi_extern_c_linking ... ok
test depgraph::tests::test_cgo_export_linking ... ok
test depgraph::tests::test_connected_components ... ok
test depgraph::tests::test_cross_lang_dep_boost ... ok
test depgraph::tests::test_dependency_boost ... ok
test depgraph::tests::test_extract_definitions_arrow_functions ... ok
test depgraph::tests::test_extract_definitions_js_function ... ok
test depgraph::tests::test_extract_definitions_python ... ok
test depgraph::tests::test_extract_definitions_rust ... ok
test depgraph::tests::test_extract_definitions_ts_interface_type_enum ... ok
test depgraph::tests::test_extract_import_targets_default_import ... ok
test depgraph::tests::test_extract_import_targets_require ... ok
test depgraph::tests::test_jni_java_to_c_linking ... ok
test depgraph::tests::test_napi_linking ... ok
test depgraph::tests::test_pymethods_exports_methods ... ok
test depgraph::tests::test_pyo3_rust_to_python_linking ... ok
test depgraph::tests::test_transitive_deps ... ok
test depgraph::tests::test_wasm_bindgen_linking ... ok
test eicv::tests::atomic_decompose_splits_conjunctions ... ok
test eicv::tests::atomic_decompose_splits_sentences ... ok
test eicv::tests::certificate_serializes ... ok
test eicv::tests::empty_claim ... ok
test eicv::tests::empty_evidence ... ok
test eicv::tests::hallucinated_claim ... ok
test eicv::tests::profiles_affect_decision ... ok
test eicv::tests::supported_claim ... ok
test eicv::tests::token_overlap_disjoint ... ok
test eicv::tests::token_overlap_identical ... ok
test eicv_suppressor::tests::annotate_mode_appends_warning ... ok
test eicv_suppressor::tests::audit_mode_no_change ... ok
test eicv_suppressor::tests::convenience_function ... ok
test eicv_suppressor::tests::extract_claims_basic ... ok
test eicv_suppressor::tests::result_serializes ... ok
test eicv_suppressor::tests::strict_mode_suppresses ... ok
test entropy::tests::test_belief_novelty_empty_corpus_is_fully_novel ... ok
test entropy::tests::test_belief_novelty_known_high_confidence_drops ... ok
test entropy::tests::test_belief_novelty_low_confidence_stays_informative ... ok
test entropy::tests::test_belief_novelty_unrelated_belief_stays_novel ... ok
test entropy::tests::test_boilerplate_detection ... ok
test entropy::tests::test_bpb_boilerplate_lower ... ok
test entropy::tests::test_bpb_empty ... ok
test entropy::tests::test_bpb_quality_combines_density_and_uniqueness ... ok
test entropy::tests::test_bpb_range ... ok
test entropy::tests::test_bpb_uniform_char ... ok
test entropy::tests::test_conditional_score_empty_beliefs_equals_information_score ... ok
test entropy::tests::test_conditional_score_known_content_discounted_to_floor ... ok
test entropy::tests::test_conditional_score_novel_beats_known ... ok
test entropy::tests::test_entropy_divergence_low_for_code ... ok
test entropy::tests::test_entropy_divergence_nonnegative ... ok
test entropy::tests::test_entropy_identical_chars ... ok
test entropy::tests::test_entropy_increases_with_diversity ... ok
test entropy::tests::test_multiscale_long_fragment_discriminates ... ok
test entropy::tests::test_multiscale_short_fragment_uses_bigrams ... ok
test entropy::tests::test_noise_penalty_in_information_score ... ok
test entropy::tests::test_redundancy_identical ... ok
test entropy::tests::test_renyi_entropy_empty ... ok
test entropy::tests::test_renyi_entropy_uniform ... ok
test entropy::tests::test_renyi_leq_shannon ... ok
test fragment::tests::test_ebbinghaus_half_life ... ok
test fragment::tests::test_relevance_scoring ... ok
test fragment::tests::test_softcap_properties ... ok
test guardrails::tests::test_critical_files ... ok
test guardrails::tests::test_feedback_tracker ... ok
test guardrails::tests::test_raven_ucb_convergence ... ok
test guardrails::tests::test_safety_signals ... ok
test guardrails::tests::test_task_classification ... ok
test guardrails::tests::test_ucb_variance_tracking ... ok
test health::tests::test_arch_violation_domain_imports_api ... ok
test health::tests::test_code_health_decreases_with_clones ... ok
test health::tests::test_code_health_perfect_score ... ok
test health::tests::test_empty_codebase_no_panic ... ok
test health::tests::test_exact_clone_detected ... ok
test health::tests::test_god_file_detection ... ok
test health::tests::test_health_grade_mapping ... ok
test health::tests::test_naming_issue_python_camel_case ... ok
test health::tests::test_same_file_not_cloned ... ok
test health::tests::test_snake_case_conversion ... ok
test hierarchical::tests::test_budget_allocation ... ok
test hierarchical::tests::test_empty_fragments ... ok
test hierarchical::tests::test_extract_oneliner_caps_at_6 ... ok
test hierarchical::tests::test_hierarchical_compress_end_to_end ... ok
test hierarchical::tests::test_identify_cluster_follows_deps ... ok
test hierarchical::tests::test_level1_generates_oneliners ... ok
test hierarchical::tests::test_pagerank_hub_gets_highest ... ok
test hierarchical::tests::test_submodular_diversity ... ok
test knapsack::tests::test_feedback_affects_selection ... ok
test knapsack::tests::test_knapsack_selects_optimal ... ok
test knapsack::tests::test_quantized_dp_respects_real_token_budget ... ok
test knapsack::tests::test_small_fragments_not_penalized ... ok
test knapsack::tests::test_soft_bisection_respects_budget ... ok
test knapsack::tests::test_soft_bisection_selects_optimal ... ok
test knapsack::tests::test_temperature_transition ... ok
test knapsack_sds::ordering_tests::ordering_is_deterministic ... ok
test knapsack_sds::ordering_tests::pinned_fragments_keep_their_position ... ok
test knapsack_sds::ordering_tests::relevance_wins_over_density_in_returned_order ... ok
test knapsack_sds::tests::test_budget_respected ... ok
test knapsack_sds::tests::test_diversity_penalizes_duplicates ... ok
test knapsack_sds::tests::test_diversity_score_range ... ok
test knapsack_sds::tests::test_empty_fragments ... ok
test knapsack_sds::tests::test_fast_path_selects_all_when_budget_generous ... ok
test knapsack_sds::tests::test_feedback_multiplier_affects_selection ... ok
test knapsack_sds::tests::test_fingerprintless_fragments_are_not_mutual_duplicates ... ok
test knapsack_sds::tests::test_multi_resolution_fits_more ... ok
test knapsack_sds::tests::test_pinned_always_included ... ok
test knapsack_sds::tests::test_reference_resolution_very_cheap ... ok
test knapsack_sds::tests::test_resolution_preference_by_budget ... ok
test knapsack_sds::tests::test_single_fragment_selected ... ok
test learning::tests::optimizer_returns_profile_after_three_episodes ... ok
test learning::tests::task_profiles_include_priors_and_bucketed_counts ... ok
test lsh::tests::test_context_scorer_ordering ... ok
test lsh::tests::test_insert_and_query_finds_exact_match ... ok
test lsh::tests::test_no_duplicates_in_results ... ok
test lsh::tests::test_remove_not_returned ... ok
test lsh::tests::test_scale_1k_query_returns_small_set ... ok
test lsh::tests::test_similar_fingerprints_found ... ok
test prism::tests::test_4d_backward_compat_api ... ok
test prism::tests::test_5d_condition_number_increases_with_resonance ... ok
test prism::tests::test_5d_cross_correlation_emergence ... ok
test prism::tests::test_5d_from_4d_preserves_covariance ... ok
test prism::tests::test_5d_non_diagonal_eigendecomposition ... ok
test prism::tests::test_5d_resonance_damping ... ok
test prism::tests::test_anisotropic_shaping_dampens_noise_4d ... ok
test prism::tests::test_jacobi_eigendecomposition_5d ... ok
test prism::tests::test_jacobi_eigendecomposition_diagonal_4d ... ok
test prism::tests::test_spectral_energy_sums_to_one ... ok
test prism::tests::test_sym_matrix4_array_roundtrip ... ok
test query::tests::test_analyze_query_full ... ok
test query::tests::test_analyze_query_vague_needs_refinement ... ok
test query::tests::test_idf_penalizes_common_terms ... ok
test query::tests::test_key_term_extraction_removes_stopwords ... ok
test query::tests::test_key_terms_prefers_specific_tokens ... ok
test query::tests::test_refinement_adds_context_terms ... ok
test query::tests::test_refinement_noop_on_empty_fragments ... ok
test query::tests::test_specific_query_low_score ... ok
test query::tests::test_stopwords_filtered ... ok
test query::tests::test_vague_query_high_score ... ok
test query_persona::tests::test_birth_on_empty ... ok
test query_persona::tests::test_build_query_features ... ok
test query_persona::tests::test_death_by_decay ... ok
test query_persona::tests::test_different_queries_can_birth ... ok
test query_persona::tests::test_fusion_identical_archetypes ... ok
test query_persona::tests::test_manifold_stats ... ok
test query_persona::tests::test_max_archetypes_cap ... ok
test query_persona::tests::test_mmd_identical_clouds ... ok
test query_persona::tests::test_per_archetype_weight_learning ... ok
test query_persona::tests::test_pitman_yor_probability ... ok
test query_persona::tests::test_rbf_kernel_distance ... ok
test query_persona::tests::test_rbf_kernel_self ... ok
test query_persona::tests::test_repeated_assignment_converges ... ok
test resonance::tests::test_chapman_estimator_accuracy ... ok
test resonance::tests::test_consolidation_near_duplicates ... ok
test resonance::tests::test_consolidation_pinned_exempt ... ok
test resonance::tests::test_coverage_high_overlap ... ok
test resonance::tests::test_coverage_low_overlap ... ok
test resonance::tests::test_coverage_monotone_in_overlap ... ok
test resonance::tests::test_coverage_zero_selected ... ok
test resonance::tests::test_resonance_cold_start_zero ... ok
test resonance::tests::test_resonance_decay ... ok
test resonance::tests::test_resonance_multiple_partners ... ok
test resonance::tests::test_resonance_negative_suppression ... ok
test resonance::tests::test_resonance_positive_reinforcement ... ok
test resonance::tests::test_resonance_symmetric ... ok
test rnr::tests::empty_claim_returns_one ... ok
test rnr::tests::fully_grounded_claim ... ok
test rnr::tests::invented_entities_penalized ... ok
test rnr::tests::no_overlap ... ok
test sast::tests::test_angular_bypass_security ... ok
test sast::tests::test_angular_innerhtml_binding ... ok
test sast::tests::test_c_inherits_cpp_rules ... ok
test sast::tests::test_cpp_buffer_overflow_strcpy ... ok
test sast::tests::test_cpp_format_string ... ok
test sast::tests::test_cpp_gets ... ok
test sast::tests::test_cpp_system_injection ... ok
test sast::tests::test_cs_binaryformatter ... ok
test sast::tests::test_cs_process_start ... ok
test sast::tests::test_cs_sql_injection ... ok
test sast::tests::test_css_expression ... ok
test sast::tests::test_css_javascript_url ... ok
test sast::tests::test_debug_true_flagged ... ok
test sast::tests::test_empty_file_zero_risk ... ok
test sast::tests::test_hardcoded_password_critical ... ok
test sast::tests::test_hardcoded_secret_redacted_in_line_content ... ok
test sast::tests::test_hardcoded_secret_still_flagged_in_markdown ... ok
test sast::tests::test_html_javascript_uri ... ok
test sast::tests::test_html_onerror_xss ... ok
test sast::tests::test_jwt_algorithms_none ... ok
test sast::tests::test_localstorage_token ... ok
test sast::tests::test_md5_password_critical ... ok
test sast::tests::test_non_secret_finding_preserves_line_content ... ok
test sast::tests::test_nosec_suppresses_finding ... ok
test sast::tests::test_openai_key_flagged ... ok
test sast::tests::test_openai_key_redacted_not_leaked ... ok
test sast::tests::test_os_system_flagged ... ok
test sast::tests::test_path_traversal_not_flagged_in_markdown ... ok
test sast::tests::test_path_traversal_still_flagged_in_python ... ok
test sast::tests::test_php_eval ... ok
test sast::tests::test_php_sql_injection ... ok
test sast::tests::test_php_unserialize ... ok
test sast::tests::test_php_xss ... ok
test sast::tests::test_pickle_loads_critical ... ok
test sast::tests::test_postmessage_star_origin ... ok
test sast::tests::test_risk_score_increases_with_severity ... ok
test sast::tests::test_rust_unsafe_with_safety_comment_not_flagged ... ok
test sast::tests::test_rust_unsafe_without_safety_comment ... ok
test sast::tests::test_sql_injection_taint_aware ... ok
test sast::tests::test_svelte_html_tag ... ok
test sast::tests::test_swift_nscoding_deserialization ... ok
test sast::tests::test_swift_userdefaults_password ... ok
test sast::tests::test_taint_propagation ... ok
test sast::tests::test_target_blank_with_noopener_suppressed ... ok
test sast::tests::test_target_blank_without_noopener ... ok
test sast::tests::test_test_file_lower_confidence ... ok
test sast::tests::test_vue_file_detects_js_rules ... ok
test sast::tests::test_vue_v_html ... ok
test sast::tests::test_xss_innerhtml_flagged ... ok
test sast::tests::test_yaml_load_without_loader ... ok
test sast::tests::test_yaml_safe_load_not_flagged ... ok
test semantic_dedup::tests::test_dedup_stats ... ok
test semantic_dedup::tests::test_empty_input ... ok
test semantic_dedup::tests::test_redundant_fragments_deduplicated ... ok
test semantic_dedup::tests::test_three_way_dedup ... ok
test semantic_dedup::tests::test_threshold_sensitivity ... ok
test semantic_dedup::tests::test_unique_fragments_kept ... ok
test simhash_wide::tests::golden_vector_pins_persisted_hash_contract ... ok
test simhash_wide::tests::identical_text_is_identical_at_every_width ... ok
test simhash_wide::tests::noise_floor_follows_one_over_sqrt_bits ... ok
test cache::tests::test_bench_mutation_stress ... ok
test simhash_wide::tests::widths_versions_and_truncated_records_are_not_comparable ... ok
test skeleton::tests::test_cpp_skeleton ... ok
test skeleton::tests::test_css_skeleton ... ok
test skeleton::tests::test_go_skeleton ... ok
test skeleton::tests::test_html_skeleton ... ok
test skeleton::tests::test_java_skeleton ... ok
test skeleton::tests::test_js_skeleton ... ok
test skeleton::tests::test_php_skeleton ... ok
test skeleton::tests::test_python_skeleton ... ok
test skeleton::tests::test_ruby_skeleton ... ok
test skeleton::tests::test_rust_skeleton ... ok
test skeleton::tests::test_short_content_returns_none ... ok
test skeleton::tests::test_skeleton_too_similar_returns_none ... ok
test skeleton::tests::test_ts_interface_kept ... ok
test skeleton::tests::test_unknown_lang_returns_none ... ok
test skeleton::tests::test_vue_sfc_skeleton ... ok
test trajectory::tests::classifies_rephrase_with_same_query ... ok
test trajectory::tests::expires_outside_window ... ok
test trajectory::tests::retry_mode_treats_non_rephrase_as_topic_change ... ok
test trajectory::tests::supports_proxy_ambiguous_band ... ok
test utilization::tests::test_empty_fragments ... ok
test utilization::tests::test_full_utilization ... ok
test utilization::tests::test_identifier_overlap_weighted_higher ... ok
test utilization::tests::test_partial_utilization ... ok
test utilization::tests::test_zero_utilization ... ok
test work_graph::tests::clean_repo_is_null_control ... ok
test work_graph::tests::completion_requires_verification ... ok
test work_graph::tests::contradicted_claim_never_becomes_trusted_fact ... ok
test work_graph::tests::dirty_repo_creates_in_progress_workstream ... ok
test work_graph::tests::event_merge_is_commutative_and_deduplicated ... ok
test work_graph::tests::explicit_task_without_durable_changes_is_planned ... ok
test work_graph::tests::failing_verification_blocks_work ... ok
test work_graph::tests::graph_bound_handoff_verification_rejects_stale_or_foreign_snapshots ... ok
test work_graph::tests::handoff_commitment_is_stable_and_detects_mutation ... ok
test work_graph::tests::lower_trust_observation_cannot_downgrade_verified_completion ... ok
test work_graph::tests::overlapping_parallel_leases_are_reported_but_not_locked ... ok
test work_graph::tests::persisted_document_detects_tampering ... ok
test work_graph::tests::persisted_graph_rejects_duplicate_events_even_if_payload_is_otherwise_valid ... ok
test work_graph::tests::resume_prioritizes_verified_evidence ... ok
test work_graph::tests::semantically_unordered_observations_have_identical_commitments ... ok
test work_graph::tests::sibling_workstreams_do_not_leak_through_shared_agent_hub ... ok
test work_graph::tests::status_without_sufficient_supporting_evidence_is_rejected ... ok
test cache::tests::test_bench_multi_workload_hit_rate ... ok
test cache::tests::test_bench_throughput ... ok
test simhash_wide::tests::wide_fingerprints_separate_duplicates_from_strangers ... ok

test result: ok. 439 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 3.69s

== cargo clippy ==
[1m[92m    Checking[0m typenum v1.20.1
[1m[92m    Checking[0m cfg-if v1.0.4
[1m[92m    Checking[0m crossbeam-utils v0.8.22
[1m[92m    Checking[0m serde_core v1.0.229
[1m[92m    Checking[0m const-oid v0.10.2
[1m[92m    Checking[0m simd-adler32 v0.3.10
[1m[92m    Checking[0m adler2 v2.0.1
[1m[92m    Checking[0m miniz_oxide v0.8.9
[1m[92m    Checking[0m crossbeam-epoch v0.9.20
[1m[92m    Checking[0m crossbeam-deque v0.8.7
[1m[92m    Checking[0m crc32fast v1.5.0
[1m[92m    Checking[0m rayon-core v1.13.0
[1m[92m    Checking[0m libc v0.2.189
[1m[92m    Checking[0m hybrid-array v0.4.14
[1m[92m    Checking[0m generic-array v0.14.7
[1m[92m    Checking[0m crypto-common v0.2.2
[1m[92m    Checking[0m crypto-common v0.1.7
[1m[92m    Checking[0m block-buffer v0.10.4
[1m[92m    Checking[0m block-buffer v0.12.1
[1m[92m    Checking[0m digest v0.10.7
[1m[92m    Checking[0m zmij v1.0.23
[1m[92m    Checking[0m bitflags v2.13.1
[1m[92m    Checking[0m cpufeatures v0.2.17
[1m[92m    Checking[0m itoa v1.0.18
[1m[92m    Checking[0m either v1.17.0
[1m[92m    Checking[0m linux-raw-sys v0.12.1
[1m[92m    Checking[0m memchr v2.8.3
[1m[92m    Checking[0m digest v0.11.3
[1m[92m    Checking[0m rayon v1.12.0
[1m[92m    Checking[0m md-5 v0.11.0
[1m[92m    Checking[0m serde v1.0.229
[1m[92m    Checking[0m rustix v1.1.4
[1m[92m    Checking[0m serde_json v1.0.151
[1m[92m    Checking[0m sha2 v0.10.9
[1m[92m    Checking[0m getrandom v0.4.3
[1m[92m    Checking[0m flate2 v1.1.9
[1m[92m    Checking[0m once_cell v1.21.4
[1m[92m    Checking[0m fastrand v2.5.0
[1m[92m    Checking[0m tempfile v3.27.0
[1m[92m    Checking[0m entroly-engine v1.0.78 (/home/runner/work/entroly/entroly/entroly-engine)
[1m[92m    Finished[0m `dev` profile [unoptimized + debuginfo] target(s) in 9.40s

fmt=1 test=0 clippy=0
