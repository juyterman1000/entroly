# Entroly v1.0.53

Entroly 1.0.53 is a focused registry-publication and release-hardening patch.

## Highlights

- publishes the canonical MCP server identity `io.github.juyterman1000/entroly`
- fixes the MCP Registry description schema bound
- verifies the canonical GitHub repository, PyPI package, npm package, runtime hints, stdio transport, and `serve` startup contract
- preserves GitHub OIDC-only registry publication with no reusable registry credential
- includes the Context Efficiency Frontier benchmark contract
- updates Hatchling compatibility and Rust regex lockfiles

## Registry packages

- PyPI: `entroly==1.0.53` via `uvx entroly serve`
- npm: `entroly-mcp@1.0.53` via `npx entroly-mcp serve`

The release workflow verifies the exact public MCP Registry listing after publication before reporting success.
