/*
 * The former static dashboard synthesized community totals and a live-looking
 * savings rate without an aggregate telemetry source. It is intentionally
 * retired. Use `entroly dashboard` for local metrics and `entroly value` for
 * an evidence-classified value receipt.
 */
